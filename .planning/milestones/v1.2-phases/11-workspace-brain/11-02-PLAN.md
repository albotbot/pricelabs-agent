---
phase: 11-workspace-brain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openclaw/workspace-pricelabs/BOOT.md
  - openclaw/workspace-pricelabs/MEMORY.md
  - openclaw/workspace-pricelabs/skills/domain-knowledge/SKILL.md
  - openclaw/workspace-pricelabs/skills/monitoring-protocols/SKILL.md
  - openclaw/workspace-pricelabs/skills/analysis-playbook/SKILL.md
  - openclaw/workspace-pricelabs/skills/optimization-playbook/SKILL.md
autonomous: true
requirements:
  - WORK-06
  - WORK-07
  - WORK-08

must_haves:
  truths:
    - "BOOT.md contains startup health check procedure (API status + online notification)"
    - "MEMORY.md has portfolio overview table structure ready for API-populated listing data"
    - "All 4 existing skill files exist as skills/<name>/SKILL.md with valid YAML frontmatter"
    - "Skill content is preserved unchanged from original files -- only frontmatter is added"
  artifacts:
    - path: "openclaw/workspace-pricelabs/BOOT.md"
      provides: "Startup health check and online notification"
      contains: "pricelabs_get_api_status"
    - path: "openclaw/workspace-pricelabs/MEMORY.md"
      provides: "Portfolio overview structure with placeholder for API data"
      contains: "Pigeon Forge"
    - path: "openclaw/workspace-pricelabs/skills/domain-knowledge/SKILL.md"
      provides: "PriceLabs platform knowledge, API catalog, STR terms"
      contains: "name: domain-knowledge"
    - path: "openclaw/workspace-pricelabs/skills/monitoring-protocols/SKILL.md"
      provides: "Daily health check protocol"
      contains: "name: monitoring-protocols"
    - path: "openclaw/workspace-pricelabs/skills/analysis-playbook/SKILL.md"
      provides: "Weekly optimization report protocol"
      contains: "name: analysis-playbook"
    - path: "openclaw/workspace-pricelabs/skills/optimization-playbook/SKILL.md"
      provides: "Pricing optimization strategies"
      contains: "name: optimization-playbook"
  key_links:
    - from: "openclaw/workspace-pricelabs/skills/*/SKILL.md"
      to: "YAML frontmatter"
      via: "name + description + user-invocable fields"
      pattern: "^---"
    - from: "openclaw/workspace-pricelabs/BOOT.md"
      to: "pricelabs MCP tools"
      via: "API status check on startup"
      pattern: "pricelabs_get_api_status"
---

<objective>
Create BOOT.md and MEMORY.md workspace files, then migrate all 4 existing skill files from flat `.md` format to agent-scoped `skills/<name>/SKILL.md` format with YAML frontmatter.

Purpose: BOOT.md defines the startup health check. MEMORY.md seeds operational memory. Skill migration enables OpenClaw auto-discovery and scopes skills to the Prism agent only.

Output: 2 workspace files + 4 migrated SKILL.md files in `openclaw/workspace-pricelabs/`.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workspace-brain/11-CONTEXT.md
@.planning/phases/11-workspace-brain/11-RESEARCH.md
@openclaw/skills/domain-knowledge.md
@openclaw/skills/monitoring-protocols.md
@openclaw/skills/analysis-playbook.md
@openclaw/skills/optimization-playbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BOOT.md and MEMORY.md</name>
  <files>
    openclaw/workspace-pricelabs/BOOT.md
    openclaw/workspace-pricelabs/MEMORY.md
  </files>
  <action>
**BOOT.md (~200-400 chars, under 500 max):**
Create a concise startup procedure. This runs only on gateway restart, NOT every turn.

Content (use the RESEARCH.md example as baseline):
1. Call pricelabs_get_api_status -- verify API connectivity
2. If API is down, send alert to the pricelabs channel
3. Send brief "Prism online" notification
4. Read today's memory file if it exists
5. Reply with NO_REPLY

Keep it terse -- this is a checklist, not prose.

**MEMORY.md (~1,000-1,500 chars to start, grows over time):**
Structure per RESEARCH.md Pattern 4:

```
# Portfolio Overview

## Listings
| Name | ID | Market | Type | Base Price |
|------|----|--------|------|------------|
<!-- Populate via pricelabs_get_listings on first boot -->

## Markets
- **Pigeon Forge, TN** -- high-demand STR market, seasonal patterns
- **Lake Winnipesaukee, NH** -- seasonal lakefront market, summer peak

## Operational Log
<!-- Agent appends observations here: metric changes, decisions, anomalies -->
<!-- Format: YYYY-MM-DD | observation -->
```

Key design decisions per CONTEXT.md:
- Listing data is NOT hardcoded -- pulled from PriceLabs API on first boot (CONTEXT.md locked decision)
- Table structure uses placeholder comment for first-boot population
- Operational log section is append-only with date stamps
- MEMORY.md is only injected in main (private) sessions, not cron/group

Do NOT pre-fill listing rows with made-up data. The structure must be ready for the agent to populate from the API.
  </action>
  <verify>
    <automated>wc -c openclaw/workspace-pricelabs/BOOT.md openclaw/workspace-pricelabs/MEMORY.md && grep -c "pricelabs_get_api_status" openclaw/workspace-pricelabs/BOOT.md && grep -c "pricelabs_get_listings" openclaw/workspace-pricelabs/MEMORY.md</automated>
  </verify>
  <done>
    - BOOT.md exists with startup health check procedure (under 500 chars)
    - BOOT.md references pricelabs_get_api_status for connectivity check
    - MEMORY.md exists with portfolio overview table structure
    - MEMORY.md has placeholder comment for API-populated listing data (not hardcoded)
    - MEMORY.md includes both markets (Pigeon Forge TN, Lake Winnipesaukee NH)
    - MEMORY.md has operational log section for rolling context
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 skill files to SKILL.md format</name>
  <files>
    openclaw/workspace-pricelabs/skills/domain-knowledge/SKILL.md
    openclaw/workspace-pricelabs/skills/monitoring-protocols/SKILL.md
    openclaw/workspace-pricelabs/skills/analysis-playbook/SKILL.md
    openclaw/workspace-pricelabs/skills/optimization-playbook/SKILL.md
  </files>
  <action>
Migrate each of the 4 existing flat skill files from `openclaw/skills/*.md` to the agent-scoped `openclaw/workspace-pricelabs/skills/<name>/SKILL.md` directory format.

For each skill file:
1. Create the directory: `openclaw/workspace-pricelabs/skills/<name>/`
2. Create SKILL.md with YAML frontmatter prepended to the existing content
3. Preserve the existing content UNCHANGED -- only add frontmatter

**Frontmatter for each file (from RESEARCH.md verified format):**

domain-knowledge/SKILL.md:
```yaml
---
name: domain-knowledge
description: PriceLabs platform knowledge, API endpoints, pricing concepts, and STR revenue management domain expertise.
user-invocable: false
---
```

monitoring-protocols/SKILL.md:
```yaml
---
name: monitoring-protocols
description: Daily portfolio health check protocol -- step-by-step monitoring procedure for listing metrics, syncs, and alerts.
user-invocable: false
---
```

analysis-playbook/SKILL.md:
```yaml
---
name: analysis-playbook
description: Weekly optimization report protocol -- portfolio KPIs, week-over-week deltas, STLY comparisons, underperformer detection.
user-invocable: false
---
```

optimization-playbook/SKILL.md:
```yaml
---
name: optimization-playbook
description: Pricing optimization strategies -- orphan day detection, demand spike capture, competitive pricing, and seasonal adjustments.
user-invocable: false
---
```

**Source files:** Read from `openclaw/skills/domain-knowledge.md`, `openclaw/skills/monitoring-protocols.md`, `openclaw/skills/analysis-playbook.md`, `openclaw/skills/optimization-playbook.md`.

**Critical checks:**
- Directory structure MUST be `skills/<name>/SKILL.md` (not `skills/<name>.md`) -- per research pitfall #3
- YAML frontmatter MUST use `---` delimiters on their own lines
- `user-invocable: false` on all 4 (reference material, not slash commands)
- Existing content after frontmatter must be byte-for-byte identical to source files
  </action>
  <verify>
    <automated>for skill in domain-knowledge monitoring-protocols analysis-playbook optimization-playbook; do echo "=== $skill ==="; head -5 "openclaw/workspace-pricelabs/skills/$skill/SKILL.md"; echo "..."; wc -c "openclaw/workspace-pricelabs/skills/$skill/SKILL.md"; done</automated>
    <manual>Spot-check that the body content after frontmatter matches the original file in openclaw/skills/</manual>
  </verify>
  <done>
    - 4 skill directories exist: skills/domain-knowledge/, skills/monitoring-protocols/, skills/analysis-playbook/, skills/optimization-playbook/
    - Each contains SKILL.md with valid YAML frontmatter (name, description, user-invocable: false)
    - Skill body content is preserved unchanged from original files
    - No flat .md files remain as the authoritative skill source (originals in openclaw/skills/ are the v1 copies; workspace versions are the v1.2 agent-scoped copies)
  </done>
</task>

</tasks>

<verification>
1. BOOT.md and MEMORY.md exist in openclaw/workspace-pricelabs/
2. BOOT.md under 500 chars, references API status check
3. MEMORY.md has listing table structure with placeholder, both markets listed
4. All 4 skill directories exist with SKILL.md files inside
5. Each SKILL.md has valid YAML frontmatter with name, description, user-invocable: false
6. Skill content matches original files (only frontmatter added)
</verification>

<success_criteria>
- BOOT.md and MEMORY.md authored and placed in workspace directory
- 4 skill files migrated to skills/<name>/SKILL.md format with YAML frontmatter
- WORK-06, WORK-07, WORK-08 requirements satisfied
- All files ready for OpenClaw auto-discovery upon agent registration (Phase 12)
</success_criteria>

<output>
After completion, create `.planning/phases/11-workspace-brain/11-02-SUMMARY.md`
</output>
