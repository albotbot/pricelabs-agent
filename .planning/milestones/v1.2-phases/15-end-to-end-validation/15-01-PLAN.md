---
phase: 15-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [E2E-04]

must_haves:
  truths:
    - "Prism and AlBot have separate workspace paths confirmed by CLI"
    - "Prism and AlBot have separate agent directories with independent auth profiles"
    - "Prism and AlBot have separate session stores with no shared session keys"
    - "Prism and AlBot have separate sandbox scopes (different tool allow/deny lists)"
    - "Prism workspace has all 4 skill directories independently (not sharing AlBot's)"
    - "All pre-flight checks pass: agents listed with bindings, channels connected, cron jobs registered, sandboxes configured"
  artifacts:
    - path: ".planning/phases/15-end-to-end-validation/15-01-SUMMARY.md"
      provides: "Pre-flight check results and workspace separation verification report"
  key_links:
    - from: "openclaw agents list"
      to: "workspace paths"
      via: "CLI output shows separate workspace fields"
      pattern: "workspace-pricelabs.*workspace$"
    - from: "openclaw sandbox explain --agent pricelabs"
      to: "openclaw sandbox explain --agent main"
      via: "comparison of tool allow lists"
      pattern: "pricelabs_.*allow"
---

<objective>
Run pre-flight CLI checks to verify system state, then verify complete workspace separation between Prism and AlBot agents.

Purpose: Confirm that the multi-agent system is correctly configured before live message testing. Catch any configuration drift or regressions from Phases 11-14 before investing time in behavioral tests. Verify E2E-04 (workspace separation without removal) via CLI and filesystem inspection.

Output: Pre-flight check results and workspace separation verification documented in SUMMARY.md. All checks must pass before Plan 02 (live message testing) proceeds.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-end-to-end-validation/15-RESEARCH.md
@.planning/phases/14-permanent-cron-jobs/14-02-SUMMARY.md
@.planning/phases/13-channel-routing/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight system state verification</name>
  <files></files>
  <action>
Run the following CLI checks against the live system. Each check has expected output. If any check fails, stop and report the failure before proceeding to Task 2.

**1. Agent list with bindings:**
```bash
openclaw agents list --bindings
```
Expected: Two agents listed:
- `main` (default: true) -- main agent
- `pricelabs` -- with 2 bindings (Telegram accountId "pricelabs" + Slack peer-channel C0AH8TSNNKH)

**2. Channel connectivity probe:**
```bash
openclaw channels status --probe
```
Expected: Telegram connected (2 accounts: default + pricelabs), Slack connected

**3. Cron job list:**
```bash
openclaw cron list
```
Expected: 4 pricelabs agent cron jobs:
- daily-health-slack (21a80cc6-d1bd-44fb-ac95-77ec4592289f) -- agentId: pricelabs
- daily-health-telegram (3f14edc8-9ebb-4942-b3eb-68f9d8f0803b) -- agentId: pricelabs
- weekly-optimization-slack (06ac95eb-f9cb-4298-b58f-a0c09f3edb74) -- agentId: pricelabs
- weekly-optimization-telegram (a483d6ba-202a-4837-a290-a275d380ef1f) -- agentId: pricelabs
Plus any existing AlBot cron jobs should still be registered.

**4. Pricelabs sandbox scope:**
```bash
openclaw sandbox explain --agent pricelabs
```
Expected: `pricelabs_*` in allow list, `exec` in deny list

**5. Main agent sandbox scope:**
```bash
openclaw sandbox explain --agent main
```
Expected: Different tool set than pricelabs agent. Main agent sandbox should NOT restrict pricelabs tools (they are kept per user decision).

Record each check's actual output and pass/fail status.
  </action>
  <verify>
    <automated>openclaw agents list --bindings 2>&1 | head -20 && echo "---" && openclaw cron list 2>&1 | head -30</automated>
    <manual>All 5 pre-flight checks show expected output with no errors or missing entries</manual>
  </verify>
  <done>All 5 pre-flight checks pass: 2 agents with correct bindings, channels connected, 4 pricelabs cron jobs registered, sandbox scopes correctly configured for both agents</done>
</task>

<task type="auto">
  <name>Task 2: Workspace separation verification (E2E-04)</name>
  <files></files>
  <action>
Verify complete workspace separation between Prism and AlBot. E2E-04 is "verify separation without removal" per user decision -- do NOT remove any files from AlBot's workspace.

**1. Verify separate workspace paths:**
```bash
openclaw agents list
```
Check output: pricelabs workspace = `~/.openclaw/workspace-pricelabs`, main workspace = `~/.openclaw/workspace` (different paths).

**2. Verify separate agent directories:**
```bash
ls -la ~/.openclaw/agents/pricelabs/agent/
ls -la ~/.openclaw/agents/main/agent/
```
Both must exist independently. Both must have `auth-profiles.json`.

**3. Verify Prism workspace has independent skills:**
```bash
ls ~/.openclaw/workspace-pricelabs/skills/
```
Expected: 4 directories -- `domain-knowledge/`, `monitoring-protocols/`, `analysis-playbook/`, `optimization-playbook/`

**4. Verify separate MEMORY.md files:**
```bash
ls -la ~/.openclaw/workspace-pricelabs/MEMORY.md
ls -la ~/.openclaw/workspace/MEMORY.md
```
Both must exist as separate files (not symlinks).

**5. Verify separate session stores:**
```bash
openclaw sessions list --agent pricelabs
openclaw sessions list --agent main
```
Different session lists, no shared session keys.

**6. Verify separate sandbox scopes:**
Compare the output already collected in Task 1 step 4 and 5. Confirm pricelabs agent has `pricelabs_*` allowed while main agent has a different tool scope.

**7. Verify AlBot workspace still has pricelabs-skills (NOT removed per user decision):**
```bash
ls ~/.openclaw/workspace/pricelabs-skills/ 2>/dev/null || echo "UNEXPECTED: pricelabs-skills directory missing"
```
Expected: Directory exists with skill files. This confirms the user's decision to keep skills in AlBot's workspace was respected.

Document all 7 checks with actual CLI output in a structured verification table.
  </action>
  <verify>
    <automated>ls ~/.openclaw/workspace-pricelabs/skills/ 2>&1 && echo "---" && ls ~/.openclaw/agents/pricelabs/agent/auth-profiles.json 2>&1 && echo "---" && ls ~/.openclaw/agents/main/agent/auth-profiles.json 2>&1</automated>
    <manual>All 7 workspace separation checks pass, confirming complete independence between agents</manual>
  </verify>
  <done>Workspace separation verified: separate workspace paths, separate agent dirs with independent auth, separate skills directories, separate MEMORY.md files, separate sessions, separate sandbox scopes. AlBot's pricelabs-skills directory preserved per user decision.</done>
</task>

</tasks>

<verification>
- All 5 pre-flight CLI checks pass with expected output
- All 7 workspace separation checks confirm complete agent independence
- No configuration drift detected from Phases 11-14
- E2E-04 satisfied: separation verified without removal
</verification>

<success_criteria>
- Pre-flight checks confirm: 2 agents with bindings, channels connected, 4 cron jobs registered, sandboxes correctly scoped
- Workspace separation confirmed across 7 dimensions: paths, dirs, auth, skills, memory, sessions, sandbox
- AlBot's pricelabs-skills directory still exists (user decision respected)
- All results documented in SUMMARY.md ready for Plan 02 to proceed
</success_criteria>

<output>
After completion, create `.planning/phases/15-end-to-end-validation/15-01-SUMMARY.md`
</output>
