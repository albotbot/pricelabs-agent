---
phase: 14-permanent-cron-jobs
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: []
autonomous: false
requirements: [CRON-01, CRON-02, CRON-03, CRON-04, CRON-05]

must_haves:
  truths:
    - "Manual trigger of daily health Slack job delivers report to #pricelabs channel with Prism persona"
    - "Manual trigger of weekly optimization Slack job delivers report to #pricelabs channel with Prism persona"
    - "Cron-delivered reports reference real portfolio data (listing names, occupancy numbers, prices)"
    - "Reports use Prism's persona and sign off with the diamond Prism signature"
  artifacts: []
  key_links:
    - from: "openclaw cron run <jobId>"
      to: "#pricelabs Slack channel (C0AH8TSNNKH)"
      via: "cron delivery announce mode"
      pattern: "Prism.*signature"
---

<objective>
Manually trigger cron jobs to verify end-to-end delivery: correct channel, correct agent persona, real data.

Purpose: Registration alone does not prove the jobs work. A manual trigger test confirms the full pipeline: cron fires -> Prism agent runs -> PriceLabs API called -> report formatted -> delivered to correct channel. Without this, issues like wrong agentId, broken delivery targeting, or prompt problems go undetected until the first scheduled run.

Output: Human-verified confirmation that cron jobs deliver correctly. Phase 14 complete.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-permanent-cron-jobs/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual trigger daily health and weekly optimization jobs via CLI</name>
  <files></files>
  <action>
Trigger manual runs of two cron jobs to verify end-to-end delivery:

**Test 1: Daily health summary (Slack)**
1. Get the job ID for "daily-health-slack" from `openclaw cron list`
2. Run `openclaw cron run <daily-health-slack-jobId>`
3. Wait for completion (check status with `openclaw cron runs --id <jobId> --limit 1`)
4. Verify the job completed successfully (status should show success, not error)

**Test 2: Weekly optimization report (Slack)**
1. Get the job ID for "weekly-optimization-slack" from `openclaw cron list`
2. Run `openclaw cron run <weekly-optimization-slack-jobId>`
3. Wait for completion (check status with `openclaw cron runs --id <jobId> --limit 1`)
4. Verify the job completed successfully

Focus on Slack deliveries because they are immediately verifiable by the user in the #pricelabs channel. Telegram delivery uses the same agent and prompt -- if Slack works, Telegram will work (the only difference is the delivery target, already verified by channel ID in registration).

**If a run fails:** Check the error in `openclaw cron runs --id <jobId> --limit 1`. Common issues:
- "agent not found" -> agentId misspelled or agent not registered
- "delivery target missing" -> wrong channel ID
- MCP timeout -> PriceLabs API issue, retry once
Report the error details for diagnosis.
  </action>
  <verify>
    <automated>openclaw cron runs --id $(openclaw cron list --json 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);const h=j.find(x=>x.name==='daily-health-slack');console.log(h?.id||'unknown')") --limit 1 2>&1 | grep -i "success\|complete" && echo "PASS" || echo "CHECK MANUALLY"</automated>
    <manual>Check #pricelabs Slack channel for delivered report with Prism persona</manual>
  </verify>
  <done>Both manual trigger runs completed successfully with no errors in openclaw cron runs output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cron deliveries in #pricelabs channel</name>
  <files></files>
  <action>
CHECKPOINT: Human verification of cron job delivery.

What was built: 4 permanent cron jobs registered targeting Prism agent. Two manual trigger tests fired (daily health + weekly optimization) delivering to #pricelabs Slack channel.

How to verify:
1. Open the #pricelabs Slack channel
2. Look for two new messages from Prism (delivered by cron):
   - **Daily Health Summary:** Should be a short table (5-10 lines) with listing names, occupancy, base prices, and flags. Red flags section at top if any issues exist. Signed with Prism's diamond signature.
   - **Weekly Optimization Report:** Should be a prose-style recommendation memo. One-line portfolio summary, then per-listing analysis (1-2 paragraphs each) for listings needing attention. Signed with Prism's diamond signature.
3. Verify the reports:
   - [ ] Uses Prism persona (not Albot)
   - [ ] References real listing names and real numbers (not placeholder data)
   - [ ] Daily health is scannable/short (dashboard feel)
   - [ ] Weekly optimization reads like a consultant recommendation
   - [ ] Both signed with diamond Prism signature
4. Run `openclaw cron list` to confirm all 4 jobs visible with agentId "pricelabs"

Resume signal: Type "approved" if reports look correct, or describe any issues with format/content/persona.
  </action>
  <verify>
    <automated>openclaw cron list 2>&1 | grep -c "pricelabs" | xargs -I{} test {} -ge 4 && echo "PASS: 4 pricelabs jobs confirmed" || echo "FAIL"</automated>
    <manual>Human confirms reports arrived in #pricelabs with Prism persona, real data, correct format</manual>
  </verify>
  <done>Human approved cron deliveries: daily health summary and weekly optimization report both arrived in #pricelabs with Prism persona, real listing data, and correct formatting</done>
</task>

</tasks>

<verification>
1. Manual trigger of daily-health-slack delivers report to #pricelabs with Prism persona and real data
2. Manual trigger of weekly-optimization-slack delivers report to #pricelabs with Prism persona and real data
3. Reports are correctly formatted per CONTEXT.md: daily is scannable table, weekly is prose recommendation
4. Both reports signed with Prism's diamond signature
5. `openclaw cron list` confirms all 4 jobs with agentId "pricelabs" (no old v1.1 jobs remain)
</verification>

<success_criteria>
- Human confirms both Slack-delivered reports arrived with correct persona, real data, and expected format
- Phase 14 requirements CRON-01 through CRON-05 satisfied
- Phase 14 complete -- ready for Phase 15 (End-to-End Validation)
</success_criteria>

<output>
After completion, create `.planning/phases/14-permanent-cron-jobs/14-02-SUMMARY.md`
</output>
