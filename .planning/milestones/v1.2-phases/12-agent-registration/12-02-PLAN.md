---
phase: 12-agent-registration
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: []  # verification only, no files changed
autonomous: false
requirements: [AGEN-02, AGEN-04, AGEN-05]

must_haves:
  truths:
    - "openclaw sandbox explain --agent pricelabs shows pricelabs_* in allowed tools"
    - "openclaw sandbox explain --agent pricelabs shows exec NOT in allowed tools"
    - "Agent responds to 'Hello, who are you?' with Prism persona (diamond signature, professional tone, NOT Albot)"
    - "Agent responds to 'Show me my listings' with real PriceLabs API data from TN/NH markets"
    - "All 28 PriceLabs tools are visible to the pricelabs agent"
  artifacts: []  # verification-only plan, no artifacts created
  key_links:
    - from: "openclaw sandbox explain --agent pricelabs"
      to: "tools.sandbox.tools.allow in agent entry"
      via: "per-agent sandbox config"
      pattern: 'pricelabs_\\*'
    - from: "openclaw agent --agent pricelabs --message"
      to: "~/.openclaw/workspace-pricelabs/SOUL.md"
      via: "workspace persona loading"
      pattern: 'Prism'
    - from: "openclaw agent --agent pricelabs --message 'Show me my listings'"
      to: "mcp.servers[].command node dist/index.js"
      via: "MCP tool invocation"
      pattern: 'pricelabs_get_listings'
---

<objective>
Verify the pricelabs agent's sandbox tool visibility (28 tools), Prism persona response, and real PriceLabs API data access via CLI. This is the acceptance testing for the entire phase.

Purpose: Confirm that the infrastructure deployed in Plan 01 actually works end-to-end -- the agent can authenticate, load its workspace brain, see its tools, and return real data.

Output: Verified, working pricelabs agent ready for channel routing in Phase 13.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-registration/12-RESEARCH.md
@.planning/phases/12-agent-registration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sandbox tool visibility and agent config</name>
  <files></files>
  <action>
Verify that the pricelabs agent sandbox is correctly configured with all 28 PriceLabs tools visible.

**Step 1: Check sandbox tool visibility**
```bash
openclaw sandbox explain --agent pricelabs
```

**Expected output must include:**
- `pricelabs_*` in the allowed tools list (this is the glob that matches all 28 tools)
- `read` in the allowed tools list (for workspace file access)
- `exec` must NOT appear in allowed tools (denied per user decision)

**Step 2: Count visible tools**
The output should show all 28 PriceLabs MCP tools are accessible. If fewer than 28 tools are visible, the MCP server config or sandbox allow list is wrong.

Known tool names (28 total from TOOLS.md):
- Read: pricelabs_get_api_status, pricelabs_get_listings, pricelabs_get_listing_prices, pricelabs_get_neighborhood_data, pricelabs_get_reservation_data, pricelabs_get_market_data
- Store: pricelabs_store_snapshot
- Retrieve: pricelabs_get_snapshots, pricelabs_get_snapshot_detail, pricelabs_compare_snapshots, pricelabs_get_change_history
- Write: pricelabs_get_listing_overrides, pricelabs_set_date_override, pricelabs_remove_date_override, pricelabs_set_date_range_override, pricelabs_set_last_minute_override, pricelabs_remove_last_minute_override, pricelabs_set_far_future_override, pricelabs_remove_far_future_override, pricelabs_sync_listing_prices, pricelabs_approve_pending_change, pricelabs_reject_pending_change
- Audit: pricelabs_get_pending_changes, pricelabs_get_operation_log, pricelabs_get_approval_status
- Config: pricelabs_get_customizations, pricelabs_get_pms_list, pricelabs_check_health

**Step 3: Verify denied tools**
Confirm that exec, process, write, edit, apply_patch, browser, canvas, nodes, gateway, sessions_spawn, sessions_send, and cron are NOT in the allowed list.

**If tools are missing (Pitfall #1 remediation):**
The per-agent sandbox allow list does NOT inherit from global. If `pricelabs_*` is missing, the agent entry in openclaw.json needs `tools.sandbox.tools.allow: ["pricelabs_*", "read"]` explicitly set. Go back to the config and fix it, then restart gateway.
  </action>
  <verify>
    <automated>openclaw sandbox explain --agent pricelabs 2>&1</automated>
    <manual>Output shows pricelabs_* in allowed tools, exec NOT in allowed tools, 28 MCP tools accessible</manual>
  </verify>
  <done>
    - `openclaw sandbox explain --agent pricelabs` shows pricelabs_* in allowed tools
    - exec and other dangerous tools are denied
    - All 28 PriceLabs MCP tools are accessible to the agent
  </done>
</task>

<task type="auto">
  <name>Task 2: Test Prism persona and real API data access via CLI</name>
  <files></files>
  <action>
Run the two-step sequential verification per user decision in CONTEXT.md.

**Test 1: Persona verification**
```bash
openclaw agent --agent pricelabs --message "Hello, who are you?"
```

**Expected persona indicators (from SOUL.md and IDENTITY.md):**
- SHOULD: Professional revenue analyst tone, references to "your portfolio" or STR revenue management
- SHOULD: Signs with "Prism" diamond signature
- SHOULD: Uses Prism intro text from IDENTITY.md
- SHOULD NOT: Casual/humorous Albot personality, emoji-heavy style, jokes
- SHOULD NOT: Generic assistant response with no domain context

**Test 2: Tool access and real data verification**
```bash
openclaw agent --agent pricelabs --message "Show me my listings"
```

**Expected data indicators:**
- Real listing data from PriceLabs API (not generic advice or placeholder data)
- Known listing names/locations from TN/NH markets (Pigeon Forge TN and/or Lake Winnipesaukee NH)
- Evidence of tool calls (pricelabs_get_listings was invoked)
- Formatted output consistent with the SOUL.md report patterns

**If persona test fails:**
- Check that workspace path in agent entry points to ~/.openclaw/workspace-pricelabs/
- Verify SOUL.md and IDENTITY.md exist in the workspace directory
- Check gateway logs for workspace loading errors

**If data test fails:**
- Run `openclaw sandbox explain --agent pricelabs` again to verify tool visibility
- Check that MCP server path is absolute: /mnt/c/Projects/pricelabs-agent/mcp-servers/pricelabs/dist/index.js
- Check that PRICELABS_API_KEY environment variable is set in the system
- Check gateway logs for MCP server spawn errors
  </action>
  <verify>
    <automated>openclaw agent --agent pricelabs --message "Hello, who are you?" 2>&1</automated>
    <manual>Response uses Prism persona (professional revenue analyst, diamond signature, NOT Albot casual tone)</manual>
  </verify>
  <done>
    - Agent responds to "Hello, who are you?" with Prism persona (professional tone, diamond signature, no Albot personality)
    - Agent responds to "Show me my listings" with real PriceLabs API data (TN/NH market listings with actual names)
    - Both tests confirm end-to-end: auth works, workspace loads, tools are visible, MCP server spawns, API returns data
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of persona and data responses</name>
  <files></files>
  <action>
**What was built:** Verified sandbox tool visibility (28 PriceLabs tools), Prism persona response, and real API data access. The pricelabs agent is fully functional.

**How to verify:**
1. Review the `openclaw sandbox explain --agent pricelabs` output -- confirm pricelabs_* tools are allowed and exec is denied
2. Review the persona response to "Hello, who are you?" -- confirm it reads as a professional revenue analyst (Prism), NOT Albot's casual personality
3. Review the data response to "Show me my listings" -- confirm real listing names from your TN/NH markets appear (not generic placeholders)
4. Optionally send your own test message to the pricelabs agent to confirm it feels right:
   ```bash
   openclaw agent --agent pricelabs --message "What's the occupancy looking like this week?"
   ```

**Resume signal:** Type "approved" if persona and data responses are correct, or describe what needs adjustment.
  </action>
  <verify>
    <automated>openclaw agent --agent pricelabs --message "Hello, who are you?" 2>&1 | head -20</automated>
  </verify>
  <done>Human confirmed Prism persona and real API data responses are correct</done>
</task>

</tasks>

<verification>
1. `openclaw sandbox explain --agent pricelabs` shows pricelabs_* allowed, exec denied
2. Persona response includes Prism identity markers (professional tone, diamond signature)
3. Persona response does NOT include Albot identity markers (casual tone, humor)
4. Data response includes real listing data from TN and/or NH markets
5. Data response demonstrates MCP tool invocation (pricelabs_get_listings)
</verification>

<success_criteria>
- All 28 PriceLabs tools visible to the pricelabs agent via sandbox explain
- Agent responds with Prism persona (revenue analyst, not Albot)
- Agent returns real PriceLabs API data (listing names from TN/NH markets)
- Human approves persona and data responses
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-registration/12-02-SUMMARY.md`
</output>
