---
phase: 10-messaging-integration
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - scripts/messaging-test-checklist.md
autonomous: false
requirements: [MSG-02, MSG-03, MSG-05]

must_haves:
  truths:
    - "User asks a portfolio question in Slack and receives a response with live PriceLabs data"
    - "User asks a portfolio question in Telegram and receives a response with live PriceLabs data"
    - "User replies 'approve' to a recommendation in Slack and agent acknowledges safely"
    - "Agent responds to natural language questions with data from MCP tools"
    - "Approval flow works end-to-end with writes disabled (safety confirmation)"
  artifacts:
    - path: "scripts/messaging-test-checklist.md"
      provides: "Structured test checklist with pass/fail recording"
      min_lines: 60
  key_links:
    - from: "Slack thread"
      to: "MCP tools (pricelabs_get_listings, pricelabs_get_prices)"
      via: "OpenClaw agent session with PriceLabs MCP server"
      pattern: "agent calls pricelabs_get_* tools in response to user message"
    - from: "Telegram chat"
      to: "MCP tools (pricelabs_get_listings, pricelabs_get_prices)"
      via: "OpenClaw agent session with PriceLabs MCP server"
      pattern: "agent calls pricelabs_get_* tools in response to user message"
    - from: "User reply 'approve'"
      to: "Agent acknowledgment with safety confirmation"
      via: "Thread reply in Slack/Telegram"
      pattern: "writes.*disabled|PRICELABS_WRITES_ENABLED.*false"
---

<objective>
Create a structured test checklist and execute live messaging interaction tests: 3 standard questions per channel (Slack and Telegram) plus a dry-run approval flow, proving MSG-02, MSG-03, and MSG-05.

Purpose: Proves that the agent actually answers portfolio questions with live data in both channels and handles the approval interaction flow correctly with write safety confirmed.

Output: scripts/messaging-test-checklist.md (filled in after manual testing)
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-messaging-integration/10-CONTEXT.md
@.planning/phases/10-messaging-integration/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create messaging interaction test checklist</name>
  <files>scripts/messaging-test-checklist.md</files>
  <action>
Create `scripts/messaging-test-checklist.md` -- a structured manual test document with specific test cases, expected behaviors, and pass/fail columns.

The checklist has 4 sections:

**Section A: Slack Question-Answer Tests (MSG-02)**

| # | Test | Message to Send | Expected Behavior | Pass/Fail | Notes |
|---|------|-----------------|-------------------|-----------|-------|
| A1 | Portfolio overview | "How is my portfolio doing?" | Agent calls pricelabs_get_listings + pricelabs_get_snapshots, responds with listing count, occupancy rates, revenue figures. Response contains real numbers, not placeholders. | | |
| A2 | Specific listing data | "What are my prices for [listing name] next week?" | Agent calls pricelabs_get_prices for the specific listing, responds with actual nightly prices for the next 7 days. Response includes dollar amounts and dates. | | |
| A3 | Recommendations | "Any recommendations?" | Agent calls pricelabs_detect_underperformers or pricelabs_get_portfolio_kpis, responds with actionable insights. Response references specific listings by name. | | |

**Section B: Telegram Question-Answer Tests (MSG-05)**

Same 3 tests (B1, B2, B3) as Section A but executed in Telegram. Same expected behaviors.

**Section C: Approval Flow Test (MSG-03)**

| # | Test | Steps | Expected Behavior | Pass/Fail | Notes |
|---|------|-------|-------------------|-----------|-------|
| C1 | Slack approval dry-run | 1. Ask agent "Any recommendations?" 2. When agent presents a recommendation, reply "approve" in thread | Agent acknowledges the approval intent but confirms writes are disabled (PRICELABS_WRITES_ENABLED=false). Does NOT execute any pricing change. Response mentions safety/writes-disabled. | | |
| C2 | Slack rejection dry-run | Reply "reject" to a recommendation thread | Agent acknowledges rejection, no action taken. | | |

**Section D: Cross-Channel Consistency**

| # | Test | Steps | Expected Behavior | Pass/Fail | Notes |
|---|------|-------|-------------------|-----------|-------|
| D1 | Same question both channels | Ask "How is my portfolio doing?" in both Slack and Telegram | Both responses contain similar data (same listing count, similar metrics). Format may differ slightly but data should be consistent. | | |

At the top of the checklist, include:
```
# Messaging Integration Test Checklist
Date: ___
Tester: ___
OpenClaw Version: ___
Agent Model: anthropic/claude-opus-4-6

## Prerequisites
- [ ] OpenClaw is running with PriceLabs agent configured
- [ ] PRICELABS_API_KEY is set (real key for live data)
- [ ] PRICELABS_WRITES_ENABLED=false (safety check)
- [ ] Slack bot is online in workspace
- [ ] Telegram bot is responding to /start

## Environment Variables Confirmed
- [ ] SLACK_APP_TOKEN set
- [ ] SLACK_BOT_TOKEN set
- [ ] TELEGRAM_BOT_TOKEN set
- [ ] SLACK_HEALTH_CHANNEL set
- [ ] TELEGRAM_HEALTH_CHAT_ID set
```

At the bottom, include a results summary section:
```
## Results Summary
Total tests: 9
Passed: ___/9
Failed: ___/9

## Requirements Coverage
- MSG-02 (Slack Q&A): Tests A1, A2, A3 -- PASS/FAIL
- MSG-03 (Slack Approval): Tests C1, C2 -- PASS/FAIL
- MSG-05 (Telegram Q&A): Tests B1, B2, B3 -- PASS/FAIL

## Bugs Found (SAFE-03)
(List any bugs discovered during testing, with fix commit hashes)
- None / [description + commit]
```
  </action>
  <verify>
    <automated>test -f scripts/messaging-test-checklist.md && wc -l scripts/messaging-test-checklist.md | awk '{if ($1 >= 60) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
    <manual>Review checklist structure: 4 sections, 9 test cases, pass/fail columns, prerequisites, results summary</manual>
  </verify>
  <done>Test checklist exists with 9 specific test cases, prerequisite checks, results summary template, and SAFE-03 bug tracking section</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute messaging interaction tests</name>
  <files>scripts/messaging-test-checklist.md</files>
  <action>
Human executes the 9 test cases from `scripts/messaging-test-checklist.md` in live Slack and Telegram channels. Claude has already created the checklist and the automated validation (Plan 01) confirmed all config is correct.

What was built: Automated validation script (Plan 01) confirmed all config, cron, and protocol references are correct. Manual test checklist created with 9 specific test cases covering Slack Q&A (MSG-02), Telegram Q&A (MSG-05), and approval flow (MSG-03).

How to verify -- execute the test checklist:

**Prerequisites (confirm before testing):**
1. OpenClaw is running with the PriceLabs agent
2. Verify PRICELABS_WRITES_ENABLED=false in the agent env
3. Confirm Slack bot is online and Telegram bot responds

**Slack Tests (MSG-02):**
1. In your Slack channel, type: "How is my portfolio doing?"
   - Expected: Agent responds with real listing data (count, occupancy %, revenue $)
2. Type: "What are my prices for [your listing name] next week?"
   - Expected: Agent responds with actual nightly prices and dates
3. Type: "Any recommendations?"
   - Expected: Agent responds with actionable insights referencing specific listings

**Telegram Tests (MSG-05):**
4. In your Telegram chat, type: "How is my portfolio doing?"
5. Type: "What are my prices for [your listing name] next week?"
6. Type: "Any recommendations?"
   - Same expected behaviors as Slack

**Approval Test (MSG-03):**
7. In Slack, after getting a recommendation, reply "approve" in the thread
   - Expected: Agent acknowledges but confirms writes are disabled (safety)
8. Reply "reject" to another recommendation
   - Expected: Agent acknowledges rejection

**Cross-Channel:**
9. Compare portfolio responses from Slack and Telegram
   - Expected: Same data, consistent metrics

Fill in Pass/Fail for each test in the checklist. Record any bugs found in the SAFE-03 section.

Resume signal: Type "approved" with test results, or describe any failures. If tests cannot be run yet (OpenClaw not available), type "deferred" to mark the checklist as ready for future testing.
  </action>
  <verify>
    <automated>test -f scripts/messaging-test-checklist.md &amp;&amp; echo "Checklist exists - manual test results recorded inside"</automated>
    <manual>User confirms 9/9 tests pass or documents failures</manual>
  </verify>
  <done>All 9 manual test cases executed and results recorded in checklist. MSG-02 (Slack Q&A), MSG-03 (approval flow), MSG-05 (Telegram Q&A) validated through live interaction.</done>
</task>

</tasks>

<verification>
1. Test checklist document exists with 9 specific test cases
2. All 3 Slack Q&A tests pass (MSG-02)
3. Slack approval dry-run works safely (MSG-03)
4. All 3 Telegram Q&A tests pass (MSG-05)
5. Cross-channel data consistency confirmed
6. Any bugs discovered documented in SAFE-03 section
</verification>

<success_criteria>
- 9/9 manual tests pass (or failures documented with remediation plan)
- Agent responds to natural language questions with real PriceLabs data in both channels
- Approval flow correctly acknowledges with write-safety confirmation
- SAFE-03 bug tracking complete (bugs fixed or clean validation documented)
</success_criteria>

<output>
After completion, create `.planning/phases/10-messaging-integration/10-02-SUMMARY.md`
</output>
