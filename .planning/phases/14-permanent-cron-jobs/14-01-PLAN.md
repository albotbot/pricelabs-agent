---
phase: 14-permanent-cron-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openclaw/cron/jobs.json
autonomous: true
requirements: [CRON-01, CRON-02, CRON-03, CRON-04, CRON-05]

must_haves:
  truths:
    - "openclaw cron list shows 4 jobs with agentId pricelabs"
    - "Daily health jobs scheduled at 7 AM CST (0 7 * * *) with Slack at stagger 0 and Telegram at stagger 30s"
    - "Weekly optimization jobs scheduled at Monday 8 AM CST (0 8 * * 1) with Slack at stagger 0 and Telegram at stagger 30s"
    - "All jobs deliver to dedicated channels -- Slack: C0AH8TSNNKH, Telegram: Prism bot chat ID"
    - "All jobs persist across gateway restarts (no deleteAfterRun flag)"
    - "Old v1.1 cron jobs removed from live system (no duplicate reports)"
  artifacts:
    - path: "openclaw/cron/jobs.json"
      provides: "4 v1.2 cron job definitions with agentId, delivery targets, prompts, schedules"
      contains: "agentId.*pricelabs"
  key_links:
    - from: "openclaw/cron/jobs.json"
      to: "live ~/.openclaw/cron/jobs.json"
      via: "openclaw cron add CLI registration"
      pattern: "agentId.*pricelabs"
    - from: "jobs.json delivery.to"
      to: "Phase 13 dedicated channels"
      via: "C0AH8TSNNKH (Slack) and Prism bot chat ID (Telegram)"
      pattern: "C0AH8TSNNKH"
---

<objective>
Replace the 4 v1.1 cron jobs with 4 new v1.2 jobs targeting the pricelabs agent, delivering to dedicated channels with updated schedules and streamlined prompts.

Purpose: The existing v1.1 cron jobs lack `agentId` (defaulting to main agent), deliver to old generic channels, use verbose prompts, and run on the wrong schedule. v1.2 jobs fix all four issues: target Prism, deliver to dedicated channels, use outcome-focused prompts, and run at user-specified times (7 AM daily, 8 AM Monday).

Output: Updated `openclaw/cron/jobs.json` in repo + 4 registered jobs on live system verified via `openclaw cron list`.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-permanent-cron-jobs/14-RESEARCH.md
@.planning/phases/14-permanent-cron-jobs/14-CONTEXT.md
@.planning/phases/13-channel-routing/13-02-SUMMARY.md
@openclaw/cron/jobs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace repo jobs.json with 4 v1.2 cron job definitions</name>
  <files>openclaw/cron/jobs.json</files>
  <action>
Replace the entire contents of `openclaw/cron/jobs.json` with 4 new v1.2 job definitions. Each job MUST include `"agentId": "pricelabs"` at the top level.

**Job 1: daily-health-slack**
- `name`: "daily-health-slack"
- `agentId`: "pricelabs"
- `schedule`: kind "cron", expr "0 7 * * *", tz "America/Chicago", staggerMs 0
- `sessionTarget`: "isolated"
- `wakeMode`: "next-heartbeat"
- `payload`: kind "agentTurn", model "opus", thinking "high"
- `payload.message`: Outcome-focused daily health prompt (see below)
- `delivery`: mode "announce", channel "slack", to "C0AH8TSNNKH", bestEffort true

**Job 2: daily-health-telegram**
- Same as Job 1 except:
- `name`: "daily-health-telegram"
- `schedule.staggerMs`: 30000
- `delivery`: channel "telegram", to "${TELEGRAM_PRISM_CHAT_ID}" (placeholder -- Task 2 will discover and replace the actual chat ID)

**Job 3: weekly-optimization-slack**
- `name`: "weekly-optimization-slack"
- `agentId`: "pricelabs"
- `schedule`: kind "cron", expr "0 8 * * 1", tz "America/Chicago", staggerMs 0
- `sessionTarget`: "isolated"
- `wakeMode`: "next-heartbeat"
- `payload`: kind "agentTurn", model "opus", thinking "high"
- `payload.message`: Outcome-focused weekly optimization prompt (see below)
- `delivery`: mode "announce", channel "slack", to "C0AH8TSNNKH", bestEffort true

**Job 4: weekly-optimization-telegram**
- Same as Job 3 except:
- `name`: "weekly-optimization-telegram"
- `schedule.staggerMs`: 30000
- `delivery`: channel "telegram", to "${TELEGRAM_PRISM_CHAT_ID}" (placeholder -- Task 2 will discover and replace)

**Daily Health Prompt** (outcome-focused, ~400 chars):
```
Produce the daily portfolio health summary. Follow your monitoring-protocols skill for data gathering.

Output format -- scannable morning dashboard (5-10 lines total):

1. RED FLAGS (if any): Lead with urgent issues -- 0% occupancy in the next 7 days, anomalous prices (>20% deviation from recommended base), stale syncs (>48h since last push). If no red flags, skip this section entirely.

2. PORTFOLIO TABLE:
Listing | Occ (30d) | Base Price | Flag
Each listing gets one row. Flag column: blank if healthy, short note if attention needed (e.g., "low occ", "stale sync", "price drift").

3. Sign off with your signature.

This is a dashboard glance, not a document. Keep it tight.
```

**Weekly Optimization Prompt** (outcome-focused, ~500 chars):
```
Produce the weekly pricing optimization report. Follow your analysis-playbook and optimization-playbook skills for data gathering and analysis.

Output format -- consultant's recommendation memo:

1. ONE-LINE SUMMARY: How the portfolio looks this week in one sentence.

2. LISTINGS NEEDING ATTENTION (ranked by urgency):
For each listing that needs a pricing change, write 1-2 paragraphs covering:
- What is happening (the metric that triggered attention)
- Why it matters (revenue impact, market context)
- What you recommend (specific pricing action with numbers)
- Skip listings that are performing well -- only cover those needing changes.

3. If no listings need attention, say so briefly and highlight the strongest performer.

4. Sign off with your signature.

Focus on actionable recommendations, not retrospective trends. This should read like a revenue manager's Monday briefing.
```

IMPORTANT: Use `${TELEGRAM_PRISM_CHAT_ID}` as a placeholder in the Telegram `delivery.to` fields. Task 2 will discover the actual chat ID and update both the repo file and the live registration.

Ensure valid JSON: no trailing commas, proper escaping of newlines in prompt strings, consistent formatting.
  </action>
  <verify>
    <automated>node -e "const j=require('./openclaw/cron/jobs.json'); if(j.length!==4) throw 'Expected 4 jobs'; j.forEach((x,i)=>{ if(x.agentId!=='pricelabs') throw 'Job '+i+' missing agentId'; if(!x.delivery.bestEffort) throw 'Job '+i+' missing bestEffort'; }); console.log('4 jobs, all with agentId:pricelabs and bestEffort:true')"</automated>
    <manual>Review jobs.json: 4 entries, daily jobs at "0 7 * * *", weekly at "0 8 * * 1", Slack to C0AH8TSNNKH, Telegram placeholder present</manual>
  </verify>
  <done>openclaw/cron/jobs.json contains exactly 4 v1.2 job definitions with agentId "pricelabs", correct schedules (7 AM daily, 8 AM Monday), correct Slack channel ID C0AH8TSNNKH, Telegram placeholder, outcome-focused prompts, bestEffort true, and isolated sessions</done>
</task>

<task type="auto">
  <name>Task 2: Discover Telegram chat ID, remove old jobs, register new jobs on live system</name>
  <files>openclaw/cron/jobs.json</files>
  <action>
This task requires live system access. Execute these steps in order:

**Step 1: Discover Prism Telegram bot chat ID**

Find the Telegram chat ID for DMs between the user and @Prism_Price_Bot. Try these methods in order:
1. `openclaw sessions list --agent pricelabs` -- look for a Telegram session key containing the chat ID
2. Check gateway logs: `journalctl -u openclaw-gateway --since "2026-02-27" | grep -i "telegram.*prism\|pricelabs.*telegram\|chat_id"` (or similar)
3. `openclaw channels resolve --channel telegram --kind dm` if available
4. As a fallback, the user's Telegram numeric ID is likely the chat ID (it was used during Phase 13 pairing)

Once discovered, update the `${TELEGRAM_PRISM_CHAT_ID}` placeholders in `openclaw/cron/jobs.json` with the actual numeric chat ID. This updates both Telegram job entries (daily-health-telegram and weekly-optimization-telegram).

**Step 2: Audit existing live cron jobs**

Run `openclaw cron list` to see all currently registered cron jobs. Identify all PriceLabs-related v1.1 jobs (they will have names like "daily-portfolio-health-slack", "daily-portfolio-health-telegram", "weekly-optimization-report-slack", "weekly-optimization-report-telegram" or similar, and will NOT have `agentId`).

**Step 3: Remove old v1.1 jobs**

For each identified v1.1 job, run `openclaw cron remove <jobId>`. Verify removal with `openclaw cron list` after each removal.

**Step 4: Register 4 new v1.2 jobs**

Register each job using `openclaw cron add` CLI with the `--agent pricelabs` flag. Reference the exact values from the updated `openclaw/cron/jobs.json`.

For each job, the CLI command pattern is:
```bash
openclaw cron add \
  --name "<name>" \
  --cron "<expr>" \
  --tz "America/Chicago" \
  --stagger <0 or 30s> \
  --session isolated \
  --message "<prompt from jobs.json>" \
  --model opus \
  --thinking high \
  --agent pricelabs \
  --announce \
  --channel <slack or telegram> \
  --to "<channel-id>"
```

Register in this order:
1. daily-health-slack (stagger 0)
2. daily-health-telegram (stagger 30s)
3. weekly-optimization-slack (stagger 0)
4. weekly-optimization-telegram (stagger 30s)

**Step 5: Verify all 4 jobs registered**

Run `openclaw cron list` and verify:
- Exactly 4 PriceLabs jobs exist (no old ones, no duplicates)
- All 4 show `agentId: "pricelabs"`
- Daily health schedule is `0 7 * * *`
- Weekly optimization schedule is `0 8 * * 1`
- Slack jobs deliver to C0AH8TSNNKH
- Telegram jobs deliver to the discovered chat ID

**If Telegram chat ID discovery fails:** Register the 2 Slack jobs immediately (CRON-01 and CRON-03 can still be completed). For the 2 Telegram jobs, create a checkpoint asking the user to send a message to @Prism_Price_Bot so the chat ID can be discovered from the resulting session.
  </action>
  <verify>
    <automated>openclaw cron list 2>&1 | grep -c "pricelabs" | xargs -I{} test {} -ge 4 && echo "PASS: 4+ pricelabs jobs found" || echo "FAIL: fewer than 4 pricelabs jobs"</automated>
    <manual>openclaw cron list output shows 4 jobs with agentId pricelabs, correct schedules, and correct delivery targets</manual>
  </verify>
  <done>All 4 v1.2 cron jobs registered on live system with agentId "pricelabs", old v1.1 jobs removed, Telegram chat ID discovered and set in both repo jobs.json and live registration, openclaw cron list confirms 4 correct jobs</done>
</task>

</tasks>

<verification>
1. `openclaw cron list` shows exactly 4 jobs with `agentId: "pricelabs"` (CRON-01 through CRON-04)
2. No old v1.1 jobs remain (no duplicate names like "daily-portfolio-health-*")
3. Daily health jobs: schedule "0 7 * * *", tz "America/Chicago" (CRON-01, CRON-02)
4. Weekly optimization jobs: schedule "0 8 * * 1", tz "America/Chicago" (CRON-03, CRON-04)
5. Slack delivery targets: C0AH8TSNNKH (CRON-01, CRON-03)
6. Telegram delivery targets: Prism bot chat ID (not old ${TELEGRAM_HEALTH_CHAT_ID}) (CRON-02, CRON-04)
7. All jobs have bestEffort: true and sessionTarget: isolated
8. No deleteAfterRun flag on any job (CRON-05: persistence)
9. Repo `openclaw/cron/jobs.json` matches live registrations (source of truth updated)
</verification>

<success_criteria>
- 4 cron jobs visible in `openclaw cron list` with agentId "pricelabs"
- Repo `openclaw/cron/jobs.json` updated with v1.2 definitions
- Old v1.1 jobs removed from live system
- Telegram Prism bot chat ID resolved and applied
</success_criteria>

<output>
After completion, create `.planning/phases/14-permanent-cron-jobs/14-01-SUMMARY.md`
</output>
