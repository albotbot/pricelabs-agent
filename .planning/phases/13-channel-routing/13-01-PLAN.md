---
phase: 13-channel-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/openclaw.json
  - ~/.openclaw/openclaw.json.bak
autonomous: false
requirements: [CHAN-01, CHAN-02, CHAN-03]

must_haves:
  truths:
    - "Telegram config uses multi-account format with accounts.default and accounts.pricelabs"
    - "Messages sent to @Prism_Price_Bot route to the pricelabs agent and get domain-appropriate responses"
    - "Messages sent to existing AlBot Telegram bot continue routing to main agent without disruption"
  artifacts:
    - path: "~/.openclaw/openclaw.json"
      provides: "Multi-account Telegram config with two bot accounts"
      contains: "accounts"
    - path: "~/.openclaw/openclaw.json"
      provides: "Telegram accountId binding for pricelabs agent"
      contains: "bindings"
  key_links:
    - from: "channels.telegram.accounts.default"
      to: "AlBot bot token"
      via: "botToken field in default account"
      pattern: "8540404056"
    - from: "channels.telegram.accounts.pricelabs"
      to: "Prism bot token"
      via: "botToken field in pricelabs account"
      pattern: "7965547116"
    - from: "bindings[].match.accountId"
      to: "agentId: pricelabs"
      via: "accountId-based routing (tier 6)"
      pattern: "accountId.*pricelabs"
---

<objective>
Migrate Telegram from flat single-bot config to multi-account format and add the dedicated PriceLabs bot (@Prism_Price_Bot) with routing binding.

Purpose: This is the highest-risk operation in Phase 13. The flat-to-multi-account migration is a BREAKING config change that must be done in two careful steps: (A) restructure existing AlBot token into multi-account format and verify it still works, then (B) add the Prism bot and Telegram binding. AlBot Telegram must remain fully functional throughout.

Output: openclaw.json with multi-account Telegram config, both bots connected, and Telegram binding routing @Prism_Price_Bot messages to the pricelabs agent.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channel-routing/13-RESEARCH.md
@.planning/phases/12-agent-registration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Telegram to multi-account format (Phase A -- restructure only)</name>
  <files>~/.openclaw/openclaw.json</files>
  <action>
    CRITICAL: This is a BREAKING config change. Follow these steps EXACTLY:

    1. Back up openclaw.json:
       `cp ~/.openclaw/openclaw.json ~/.openclaw/openclaw.json.bak`

    2. Read the current `channels.telegram` section from openclaw.json. It currently looks like:
       ```json
       "telegram": {
         "enabled": true,
         "dmPolicy": "pairing",
         "botToken": "8540404056:AAERapYCYOl5YtGM6IutMWDXNiB-L1RTVZc",
         "groupPolicy": "allowlist",
         "streaming": "partial"
       }
       ```

    3. Restructure to multi-account format. The target is:
       ```json
       "telegram": {
         "enabled": true,
         "groupPolicy": "allowlist",
         "streaming": "partial",
         "accounts": {
           "default": {
             "botToken": "8540404056:AAERapYCYOl5YtGM6IutMWDXNiB-L1RTVZc",
             "dmPolicy": "pairing"
           }
         }
       }
       ```

       Rules:
       - REMOVE `botToken` from top level -- move into `accounts.default.botToken`
       - REMOVE `dmPolicy` from top level -- move into `accounts.default.dmPolicy`
       - KEEP `enabled`, `groupPolicy`, `streaming` at top level (base settings apply to all accounts)
       - Do NOT add the Prism bot yet -- this step only restructures the existing config
       - Use a JSON-safe merge approach -- read the full file, parse, modify in-memory, write back. Never sed/awk on production JSON.

    4. Restart gateway:
       `sudo systemctl restart openclaw-gateway.service`

    5. Wait for Telegram rate limit cooldown:
       `sleep 30`

    6. Verify AlBot still works:
       `openclaw channels status --probe`
       Expected: Telegram shows "connected" status.

    If Telegram shows disconnected or AlBot stops responding, IMMEDIATELY restore backup:
    `cp ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json && sudo systemctl restart openclaw-gateway.service`
  </action>
  <verify>
    <automated>openclaw channels status --probe | grep -i telegram</automated>
    <manual>DM the existing AlBot Telegram bot and confirm it responds normally</manual>
  </verify>
  <done>Telegram config uses multi-account format with accounts.default containing the existing bot token. AlBot Telegram continues working without disruption. openclaw channels status --probe shows Telegram connected.</done>
</task>

<task type="auto">
  <name>Task 2: Add Prism bot and Telegram binding (Phase B -- new bot)</name>
  <files>~/.openclaw/openclaw.json</files>
  <action>
    PREREQUISITE: Task 1 must be complete and AlBot verified working on Telegram.

    1. Back up the current (already migrated) config:
       `cp ~/.openclaw/openclaw.json ~/.openclaw/openclaw.json.bak`

    2. Read openclaw.json. Add the pricelabs account to `channels.telegram.accounts`:
       ```json
       "pricelabs": {
         "botToken": "7965547116:AAH6yVXqEeZogHDPbQPsjgTja3H6CEQwxV4",
         "dmPolicy": "pairing"
       }
       ```
       This goes alongside `accounts.default` (do NOT modify or remove default).

    3. Add the `bindings` array at the TOP LEVEL of openclaw.json (currently absent from config).
       Add the Telegram binding:
       ```json
       "bindings": [
         {
           "agentId": "pricelabs",
           "match": {
             "channel": "telegram",
             "accountId": "pricelabs"
           }
         }
       ]
       ```
       This is a new top-level key -- place it after `agents` or `channels` (wherever makes structural sense).
       No binding needed for AlBot -- default agent fallback handles unmatched messages.

    4. Restart gateway:
       `sudo systemctl restart openclaw-gateway.service`

    5. Wait for rate limit cooldown:
       `sleep 30`

    6. Verify both bots connect:
       `openclaw channels status --probe`
       Expected: Telegram shows both accounts connected.

    7. Verify routing table:
       `openclaw agents list --bindings`
       Expected: pricelabs agent has Telegram accountId binding.

    If anything breaks, restore backup immediately.
  </action>
  <verify>
    <automated>openclaw channels status --probe && openclaw agents list --bindings</automated>
    <manual>DM @Prism_Price_Bot and verify Prism persona responds. DM existing AlBot bot and verify it still responds as AlBot.</manual>
  </verify>
  <done>Both Telegram bots connected. Binding routes @Prism_Price_Bot messages to pricelabs agent. AlBot continues receiving messages on existing bot. openclaw agents list --bindings shows the Telegram accountId binding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Telegram routing</name>
  <files></files>
  <action>
**What was built:** Telegram multi-account migration with two bots: AlBot (default) and Prism (@Prism_Price_Bot). Binding routes Prism bot messages to the pricelabs agent.

**How to verify:**

1. DM @Prism_Price_Bot on Telegram with "Who are you?"
   - Expected: Prism persona responds (revenue analyst, diamond signature)
   - NOT expected: AlBot persona

2. DM the existing AlBot Telegram bot with "hello"
   - Expected: AlBot responds normally as before
   - NOT expected: Prism persona or no response

3. Confirm `openclaw channels status --probe` shows Telegram connected

Type "approved" if both bots route correctly, or describe issues.
  </action>
  <verify>User confirms both Telegram bots route to correct agents</verify>
  <done>Telegram routing verified by human. Prism bot routes to pricelabs agent, AlBot bot routes to main agent.</done>
</task>

</tasks>

<verification>
- `openclaw channels status --probe` shows Telegram connected with both accounts
- `openclaw agents list --bindings` shows pricelabs agent with Telegram accountId binding
- DM @Prism_Price_Bot gets Prism persona response
- DM existing AlBot bot gets AlBot persona response (no disruption)
</verification>

<success_criteria>
Telegram multi-account migration complete. Both bots connected and routing correctly. AlBot unaffected. Prism bot messages route to pricelabs agent.
</success_criteria>

<output>
After completion, create `.planning/phases/13-channel-routing/13-01-SUMMARY.md`
</output>
