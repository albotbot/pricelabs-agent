---
phase: 12-agent-registration
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: []
autonomous: false
requirements: [AGEN-02, AGEN-04, AGEN-05]

must_haves:
  truths:
    - "openclaw sandbox explain shows pricelabs_* in allowed tools for the pricelabs agent"
    - "Agent responds with Prism persona -- professional revenue analyst tone, not Albot personality"
    - "Agent can call PriceLabs MCP tools and return real API data from TN/NH markets"
    - "All 28 PriceLabs tools are visible to the agent"
    - "exec tool is NOT in the allowed tools list"
  artifacts: []
  key_links:
    - from: "openclaw sandbox explain --agent pricelabs"
      to: "pricelabs_* tool namespace"
      via: "sandbox tool allow list verification"
      pattern: "pricelabs_"
    - from: "openclaw agent --agent pricelabs --message 'Hello'"
      to: "SOUL.md persona"
      via: "agent workspace loading at message time"
      pattern: "Prism|portfolio|revenue"
    - from: "openclaw agent --agent pricelabs --message 'Show me my listings'"
      to: "pricelabs_get_listings MCP tool"
      via: "tool invocation returning real API data"
      pattern: "Pigeon Forge|Winnipesaukee|Tennessee|New Hampshire"
---

<objective>
Verify the pricelabs agent registration is fully functional: sandbox tool visibility, Prism persona response, and real PriceLabs API data access via all 28 MCP tools.

Purpose: Registration without verification is incomplete. Each critical integration point must be explicitly confirmed before declaring Phase 12 done.
Output: Verified working agent with correct persona and full tool access.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-registration/12-RESEARCH.md
@.planning/phases/12-agent-registration/12-CONTEXT.md
@.planning/phases/12-agent-registration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sandbox tool visibility and agent listing</name>
  <files></files>
  <action>
    Three verification checks (all must pass):

    1. **Verify agent is registered:**
       ```bash
       openclaw agents list
       ```
       Expected: Output includes "pricelabs" in the agent list. Should show 9 total agents (8 NGA + 1 pricelabs).

    2. **Verify sandbox tool allow list (MOST IMPORTANT -- prevents Pitfall #1):**
       ```bash
       openclaw sandbox explain --agent pricelabs
       ```
       Expected output MUST show:
       - `pricelabs_*` in the allowed tools list
       - `exec` NOT in the allowed tools list
       - `read` in the allowed tools list
       Count the pricelabs tools visible. All 28 must be accessible. The 28 tools are:
       pricelabs_get_api_status, pricelabs_get_listings, pricelabs_get_listing_prices, pricelabs_get_market_data, pricelabs_get_neighborhood_data, pricelabs_get_reservations, pricelabs_get_listing_overrides, pricelabs_set_listing_override, pricelabs_delete_listing_override, pricelabs_sync_listing_prices, pricelabs_store_listing_snapshot, pricelabs_store_price_snapshot, pricelabs_store_market_snapshot, pricelabs_store_reservation_data, pricelabs_get_listing_history, pricelabs_get_price_trends, pricelabs_get_reservation_analytics, pricelabs_get_market_trends, pricelabs_get_audit_log, pricelabs_log_audit_event, pricelabs_get_portfolio_kpis, pricelabs_detect_underperformers, pricelabs_snapshot_before_write, pricelabs_record_change, pricelabs_get_change_impact, pricelabs_get_user_config, pricelabs_set_user_config, pricelabs_get_latest_snapshots.

    3. **If sandbox explain does NOT show pricelabs tools:**
       This means the per-agent sandbox allow list is not configured correctly. Check that `tools.sandbox.tools.allow` contains `["pricelabs_*", "read"]` in the agent entry. Fix the config if needed, restart gateway, and re-verify.

    **If any check fails, stop and troubleshoot before proceeding to persona verification.**
  </action>
  <verify>
    <automated>openclaw sandbox explain --agent pricelabs 2>&1 | grep -c "pricelabs_"</automated>
    <manual>Output count should be 28 (one for each PriceLabs tool). exec should NOT appear in allowed tools.</manual>
  </verify>
  <done>Pricelabs agent visible in agents list; sandbox explain shows all 28 pricelabs_* tools allowed and exec denied</done>
</task>

<task type="auto">
  <name>Task 2: Test persona and tool access via CLI messages</name>
  <files></files>
  <action>
    Two-step sequential verification via CLI (per user decision in CONTEXT.md):

    **Step 1: Persona verification**
    ```bash
    openclaw agent --agent pricelabs --message "Hello, who are you?"
    ```
    Verify the response:
    - SHOULD: Use Prism identity (professional revenue analyst tone), sign with "Prism" or diamond symbol, reference "your portfolio" or "revenue management"
    - SHOULD NOT: Be casual, humorous, or use Albot personality. No jokes, no emojis beyond the diamond signature, no informal language.
    - If the persona is wrong (Albot tone, generic assistant), check that the workspace path in openclaw.json points to `~/.openclaw/workspace-pricelabs` and that SOUL.md exists there.

    **Step 2: Tool access and real data verification**
    ```bash
    openclaw agent --agent pricelabs --message "Show me my listings"
    ```
    Verify the response:
    - MUST contain real PriceLabs API data (not placeholder or mock data)
    - MUST reference specific listings from the TN/NH markets (Pigeon Forge, Tennessee and/or Lake Winnipesaukee, New Hampshire)
    - MUST show actual listing names/locations that match the portfolio described in MEMORY.md
    - If the response is generic or says "I don't have access to tools", the MCP server config is broken. Check the `mcp.servers` block in the agent entry, verify the absolute path to dist/index.js, and ensure PRICELABS_API_KEY env var is set.

    **Troubleshooting guide if either test fails:**
    - Persona wrong → workspace path incorrect or SOUL.md missing from deployed workspace
    - No tools available → MCP server not spawning (check path), or sandbox allow list missing pricelabs_* (check sandbox explain)
    - Auth error → auth-profiles.json not copied or corrupted (re-copy from main agent)
    - Gateway error → restore backup and retry config merge
  </action>
  <verify>
    <automated>openclaw agent --agent pricelabs --message "How many listings do I have?" 2>&1 | grep -iE "listing|portfolio|pricelabs|Pigeon|Winnipesaukee|Tennessee|Hampshire"</automated>
    <manual>Response mentions specific listings and real data from PriceLabs API</manual>
  </verify>
  <done>Agent responds with Prism persona (professional revenue analyst, not Albot), references real portfolio data from TN/NH markets, and successfully calls PriceLabs MCP tools</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Prism persona and data accuracy</name>
  <files></files>
  <action>
    Human verification of the complete pricelabs agent registration. The agent should be responding as a professional revenue analyst managing your STR portfolio with access to all 28 PriceLabs MCP tools.

    Verification steps:
    1. Run `openclaw sandbox explain --agent pricelabs` -- confirm all 28 pricelabs_* tools are visible and exec is denied
    2. Run `openclaw agent --agent pricelabs --message "Hello, who are you?"` -- confirm Prism persona (professional, NOT Albot casual tone)
    3. Run `openclaw agent --agent pricelabs --message "Show me my listings"` -- confirm real listing data from your TN/NH properties
    4. Verify the 8 existing NGA agents still work: `openclaw agents list` should show 9 agents total

    Resume signal: Type "approved" if Prism responds correctly with proper persona and real data, or describe issues to fix.
  </action>
  <verify>
    <automated>openclaw agents list 2>&1 | grep -c "pricelabs"</automated>
    <manual>User confirms persona tone, data accuracy, and tool visibility</manual>
  </verify>
  <done>User has approved Prism persona response, real data access from TN/NH markets, and all 28 tool visibility</done>
</task>

</tasks>

<verification>
1. `openclaw agents list` shows pricelabs alongside 8 NGA agents (9 total)
2. `openclaw sandbox explain --agent pricelabs` shows 28 pricelabs_* tools, read tool, exec denied
3. Persona test returns professional revenue analyst response with Prism identity markers
4. Data test returns real PriceLabs API data with known listing names from TN/NH markets
5. Existing NGA agents unaffected by the addition
</verification>

<success_criteria>
- All 28 PriceLabs tools visible via sandbox explain
- exec tool denied in sandbox
- Agent responds with Prism persona (not Albot)
- Agent returns real API data when queried about listings
- User has approved persona and data accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-registration/12-02-SUMMARY.md`
</output>
