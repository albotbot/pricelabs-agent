---
phase: 12-agent-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/workspace-pricelabs/ (deployed from repo)
  - ~/.openclaw/agents/pricelabs/agent/auth-profiles.json
  - ~/.openclaw/openclaw.json
autonomous: true
requirements: [AGEN-01, AGEN-02, AGEN-03]

must_haves:
  truths:
    - "openclaw.json contains a pricelabs agent entry in agents.list[] with id 'pricelabs'"
    - "Agent directory exists at ~/.openclaw/agents/pricelabs/agent/ with auth-profiles.json"
    - "Workspace files deployed to ~/.openclaw/workspace-pricelabs/ with all bootstrap files and skills"
    - "Per-agent sandbox explicitly allows pricelabs_* tools and denies exec"
    - "Gateway restarts successfully after config merge"
  artifacts:
    - path: "~/.openclaw/openclaw.json"
      provides: "Pricelabs agent entry merged into live NGA gateway config"
      contains: '"id": "pricelabs"'
    - path: "~/.openclaw/agents/pricelabs/agent/auth-profiles.json"
      provides: "LLM authentication credentials copied from main agent"
    - path: "~/.openclaw/workspace-pricelabs/AGENTS.md"
      provides: "Operating instructions deployed to live system"
    - path: "~/.openclaw/workspace-pricelabs/SOUL.md"
      provides: "Prism persona deployed to live system"
  key_links:
    - from: "~/.openclaw/openclaw.json agents.list[].workspace"
      to: "~/.openclaw/workspace-pricelabs/"
      via: "workspace path in agent entry"
      pattern: '"workspace".*workspace-pricelabs'
    - from: "~/.openclaw/openclaw.json agents.list[].mcp.servers"
      to: "/mnt/c/Projects/pricelabs-agent/mcp-servers/pricelabs/dist/index.js"
      via: "MCP server command args"
      pattern: 'pricelabs.*dist/index.js'
    - from: "~/.openclaw/openclaw.json agents.list[].tools.sandbox.tools.allow"
      to: "pricelabs_* tool namespace"
      via: "per-agent sandbox allow list"
      pattern: '"pricelabs_\\*"'
---

<objective>
Deploy Prism workspace, create agent directory with auth, and register the pricelabs agent in the live OpenClaw gateway config with per-agent sandbox, tool access, and MCP server.

Purpose: The pricelabs agent needs to exist as a registered entity in the NGA gateway before any verification or messaging tests can run.
Output: Live gateway running with pricelabs agent entry, workspace deployed, auth profiles in place.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-registration/12-RESEARCH.md
@.planning/phases/12-agent-registration/12-CONTEXT.md
@.planning/phases/11-workspace-brain/11-01-SUMMARY.md
@.planning/phases/11-workspace-brain/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy workspace and create agent directory with auth</name>
  <files>
    ~/.openclaw/workspace-pricelabs/ (full directory)
    ~/.openclaw/agents/pricelabs/agent/auth-profiles.json
    ~/.openclaw/openclaw.json.bak
  </files>
  <action>
    Three deployment steps (order matters):

    1. **Back up the live openclaw.json** (per user decision -- rollback safety):
       ```bash
       cp ~/.openclaw/openclaw.json ~/.openclaw/openclaw.json.bak
       ```

    2. **Deploy workspace files from repo to live system:**
       ```bash
       cp -r /mnt/c/Projects/pricelabs-agent/openclaw/workspace-pricelabs/ ~/.openclaw/workspace-pricelabs/
       ```
       Verify all 8 bootstrap files AND 4 skill directories exist after copy:
       - AGENTS.md, SOUL.md, USER.md, IDENTITY.md, TOOLS.md, BOOT.md, MEMORY.md, HEARTBEAT.md
       - skills/domain-knowledge/SKILL.md, skills/monitoring-protocols/SKILL.md, skills/analysis-playbook/SKILL.md, skills/optimization-playbook/SKILL.md

    3. **Create agent directory and copy auth profiles from main agent:**
       ```bash
       mkdir -p ~/.openclaw/agents/pricelabs/agent
       cp ~/.openclaw/agents/main/agent/auth-profiles.json ~/.openclaw/agents/pricelabs/agent/
       ```
       Auth copy MUST happen immediately after directory creation (Pitfall #2 from research). Without auth-profiles.json, the agent cannot make LLM calls.
  </action>
  <verify>
    <automated>ls ~/.openclaw/workspace-pricelabs/AGENTS.md ~/.openclaw/workspace-pricelabs/SOUL.md ~/.openclaw/workspace-pricelabs/skills/domain-knowledge/SKILL.md ~/.openclaw/agents/pricelabs/agent/auth-profiles.json ~/.openclaw/openclaw.json.bak 2>&1 | grep -v "No such file"</automated>
    <manual>All 5 paths exist -- workspace files, skills, auth profile, and backup</manual>
  </verify>
  <done>Workspace deployed with all 8 bootstrap files and 4 skill directories; agent directory created with auth-profiles.json copied from main; openclaw.json backed up</done>
</task>

<task type="auto">
  <name>Task 2: Merge pricelabs agent entry into live openclaw.json and restart gateway</name>
  <files>~/.openclaw/openclaw.json</files>
  <action>
    **CRITICAL: This modifies the LIVE NGA gateway config that runs 8 existing agents. The repo's openclaw/openclaw.json is a REFERENCE TEMPLATE ONLY -- do NOT replace the live config.**

    1. **Read the live `~/.openclaw/openclaw.json`** to understand the existing `agents.list[]` structure. It contains 8 NGA agents. The pricelabs entry must be APPENDED to the end of this array.

    2. **Add the following agent entry** to the end of `agents.list[]` in the live config. Use the EXACT JSON below -- it has been verified against the research docs, user decisions, and pitfall list:

       ```json
       {
         "id": "pricelabs",
         "name": "Prism",
         "workspace": "~/.openclaw/workspace-pricelabs",
         "model": {
           "primary": "openai-codex/gpt-5.3-codex"
         },
         "identity": {
           "name": "Prism",
           "emoji": "diamond_shape_with_a_dot_inside"
         },
         "sandbox": {
           "mode": "all",
           "scope": "agent",
           "workspaceAccess": "rw"
         },
         "tools": {
           "deny": [
             "exec", "process", "write", "edit", "apply_patch",
             "browser", "canvas", "nodes", "gateway",
             "group:runtime", "group:automation",
             "sessions_spawn", "sessions_send", "cron"
           ],
           "sandbox": {
             "tools": {
               "allow": ["pricelabs_*", "read"]
             }
           },
           "fs": {
             "workspaceOnly": true
           },
           "exec": {
             "security": "deny"
           },
           "elevated": {
             "enabled": false
           }
         },
         "mcp": {
           "servers": [
             {
               "name": "pricelabs",
               "command": "node",
               "args": ["/mnt/c/Projects/pricelabs-agent/mcp-servers/pricelabs/dist/index.js"],
               "env": {
                 "PRICELABS_API_KEY": "${PRICELABS_API_KEY}",
                 "PRICELABS_BASE_URL": "https://api.pricelabs.co",
                 "PRICELABS_DB_PATH": "${HOME}/.pricelabs-agent/data.sqlite",
                 "PRICELABS_WRITES_ENABLED": "false"
               }
             }
           ]
         }
       }
       ```

    **KEY FIELD WARNINGS (from research pitfalls):**
    - `agentDir` is NOT set -- it auto-resolves to `~/.openclaw/agents/pricelabs/agent`. Do NOT add `agentsDir` (plural) -- this typo crashed the gateway 39 times.
    - `tools.sandbox.tools.allow` MUST include `pricelabs_*` -- per-agent sandbox does NOT inherit from global (Pitfall #1, v1.1 root cause repeat).
    - `model.primary` is `openai-codex/gpt-5.3-codex` per user decision (same as NGA defaults). NOT the repo's `anthropic/claude-opus-4-6`.
    - MCP server args uses ABSOLUTE path to the built dist/index.js.
    - No `instructions: [...]` field -- skills are in the workspace directory (Phase 11 migration).

    3. **Validate the JSON is well-formed** after editing. A syntax error will crash the gateway.

    4. **Restart the gateway:**
       ```bash
       openclaw gateway restart
       ```
       Wait for startup confirmation. If the gateway fails to start, immediately restore the backup:
       ```bash
       cp ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json
       openclaw gateway restart
       ```
  </action>
  <verify>
    <automated>openclaw agents list 2>&1 | grep -i pricelabs</automated>
    <manual>Gateway restarted successfully and pricelabs agent appears in agents list</manual>
  </verify>
  <done>Pricelabs agent entry merged into live openclaw.json with correct id, workspace path, model, sandbox config (workspaceAccess: rw), tool deny/allow lists (pricelabs_* allowed, exec denied), and MCP server; gateway restarted and running</done>
</task>

</tasks>

<verification>
1. `openclaw agents list` shows "pricelabs" agent
2. `ls ~/.openclaw/workspace-pricelabs/` shows all 8 bootstrap files + skills/
3. `ls ~/.openclaw/agents/pricelabs/agent/auth-profiles.json` confirms auth is in place
4. `diff ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json` shows only the added pricelabs agent entry
5. Gateway is running (no crash after restart)
</verification>

<success_criteria>
- Pricelabs agent registered in live openclaw.json agents.list[] with all required fields
- Workspace files deployed to ~/.openclaw/workspace-pricelabs/ (8 files + 4 skill directories)
- Auth profiles copied to ~/.openclaw/agents/pricelabs/agent/auth-profiles.json
- Gateway running with 9 agents (8 NGA + 1 pricelabs)
- Backup of original config exists at openclaw.json.bak
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-registration/12-01-SUMMARY.md`
</output>
