---
phase: 04-write-operations-approval-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-servers/pricelabs/src/schemas/monitoring.ts
  - mcp-servers/pricelabs/src/schemas/optimization.ts
  - mcp-servers/pricelabs/src/tools/optimization.ts
  - mcp-servers/pricelabs/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Agent can snapshot current listing state and overrides before any write operation"
    - "Snapshot is stored in audit log with action_type='snapshot' for rollback capability"
    - "Audit log accepts 'snapshot' and 'rollback' action types in addition to existing types"
  artifacts:
    - path: "mcp-servers/pricelabs/src/schemas/optimization.ts"
      provides: "Zod input schema for pricelabs_snapshot_before_write tool"
      contains: "SnapshotBeforeWriteInputSchema"
    - path: "mcp-servers/pricelabs/src/tools/optimization.ts"
      provides: "Pre-write snapshot MCP tool registration"
      exports: ["registerOptimizationTools"]
    - path: "mcp-servers/pricelabs/src/schemas/monitoring.ts"
      provides: "Extended action_type enum with snapshot and rollback"
      contains: "snapshot"
    - path: "mcp-servers/pricelabs/src/index.ts"
      provides: "Server wiring for optimization tools"
      contains: "registerOptimizationTools"
  key_links:
    - from: "mcp-servers/pricelabs/src/tools/optimization.ts"
      to: "mcp-servers/pricelabs/src/schemas/optimization.ts"
      via: "import SnapshotBeforeWriteInputSchema"
      pattern: "import.*SnapshotBeforeWriteInputSchema.*from.*schemas/optimization"
    - from: "mcp-servers/pricelabs/src/tools/optimization.ts"
      to: "mcp-servers/pricelabs/src/db/queries/audit-log.ts"
      via: "createAuditLogQueries for snapshot storage"
      pattern: "createAuditLogQueries"
    - from: "mcp-servers/pricelabs/src/index.ts"
      to: "mcp-servers/pricelabs/src/tools/optimization.ts"
      via: "import and call registerOptimizationTools"
      pattern: "registerOptimizationTools.*server.*apiClient"
---

<objective>
Create the pricelabs_snapshot_before_write MCP tool and extend the audit log action_type enum to support 'snapshot' and 'rollback' types.

Purpose: OPT-03 requires the agent to snapshot current values before every write operation for rollback capability. A dedicated MCP tool ensures snapshots are always captured (tool call is explicit and cannot be skipped by the agent forgetting a skill instruction). The audit log enum extension enables the snapshot and rollback entries to be properly typed and filterable.

Output: One new MCP tool (pricelabs_snapshot_before_write), one new schema file (optimization.ts), updated monitoring.ts schema with extended enum, updated index.ts with server wiring. Tool count goes from 23 to 24.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-write-operations-approval-workflow/04-RESEARCH.md

@mcp-servers/pricelabs/src/index.ts
@mcp-servers/pricelabs/src/schemas/monitoring.ts
@mcp-servers/pricelabs/src/tools/audit.ts
@mcp-servers/pricelabs/src/tools/analysis.ts
@mcp-servers/pricelabs/src/db/queries/audit-log.ts
@mcp-servers/pricelabs/src/services/api-client.ts
@mcp-servers/pricelabs/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend audit enum and create snapshot schema</name>
  <files>
    mcp-servers/pricelabs/src/schemas/monitoring.ts
    mcp-servers/pricelabs/src/schemas/optimization.ts
  </files>
  <action>
1. Update `mcp-servers/pricelabs/src/schemas/monitoring.ts`:
   - In `LogActionInputSchema`, extend the `action_type` enum from `["recommendation", "approval", "execution", "alert", "report"]` to include `"snapshot"` and `"rollback"` (7 total values).
   - In `GetAuditLogInputSchema`, extend the `action_type` enum to match (same 7 values).
   - Do NOT change any other fields or descriptions. Only the enum arrays change.

2. Create `mcp-servers/pricelabs/src/schemas/optimization.ts`:
   - Import z from "zod".
   - Export `SnapshotBeforeWriteInputSchema` as a z.object with these fields:
     - `listing_id`: z.string(), describe "PriceLabs listing ID to snapshot"
     - `pms`: z.string(), describe "PMS identifier (e.g., 'airbnb', 'bookingsync')"
     - `operation_type`: z.enum(["set_overrides", "update_listing", "delete_overrides"]), describe "Type of write operation about to be performed"
     - `start_date`: z.string().optional(), describe "Start date for override date range (YYYY-MM-DD). Required for set_overrides and delete_overrides."
     - `end_date`: z.string().optional(), describe "End date for override date range (YYYY-MM-DD). Required for set_overrides and delete_overrides."
     - `channel`: z.string().optional(), describe "Channel context: 'slack', 'telegram', 'cron', 'interactive'"
   - Export the inferred type: `export type SnapshotBeforeWriteInput = z.infer<typeof SnapshotBeforeWriteInputSchema>;`
   - Follow the same file structure as existing schema files (import, schema, type export).
  </action>
  <verify>Run `npx tsc --noEmit` from `mcp-servers/pricelabs/` -- zero errors. Verify the schema files import correctly.</verify>
  <done>monitoring.ts has 7-value action_type enums in both schemas. optimization.ts exists with SnapshotBeforeWriteInputSchema exported. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create snapshot tool and wire to server</name>
  <files>
    mcp-servers/pricelabs/src/tools/optimization.ts
    mcp-servers/pricelabs/src/index.ts
  </files>
  <action>
1. Create `mcp-servers/pricelabs/src/tools/optimization.ts`:
   - Follow the exact pattern from `tools/analysis.ts` (module doc comment, typed imports, exported register function).
   - Import McpServer type, Database type, PriceLabsApiClient, TtlCache, TokenBucketRateLimiter.
   - Import `SnapshotBeforeWriteInputSchema` from `../schemas/optimization.js`.
   - Import `createAuditLogQueries` from `../db/queries/audit-log.js`.
   - Import `fetchWithFallback` from `../services/fetch-with-fallback.js`.
   - Import the `Listing` and `OverrideEntry` types from `../types.js`.
   - Export function signature: `registerOptimizationTools(server: McpServer, db: Database.Database, apiClient: PriceLabsApiClient, cache: TtlCache, rateLimiter: TokenBucketRateLimiter): void`
   - Register ONE tool: `pricelabs_snapshot_before_write`
   - Tool description: "Capture current listing state (base/min/max prices, currency) and existing overrides before a write operation. MUST be called before every pricelabs_set_overrides, pricelabs_update_listings, or pricelabs_delete_overrides call. Returns a structured snapshot and stores it in the audit log for rollback capability."
   - Annotations: `{ readOnlyHint: true, destructiveHint: false, openWorldHint: true }` (it reads data and writes audit, but does not modify pricing)
   - inputSchema: `SnapshotBeforeWriteInputSchema.shape`
   - Handler logic:
     a. Fetch current listing data using `fetchWithFallback` with the listing endpoint `/v1/listings/${params.listing_id}?pms=${encodeURIComponent(params.pms)}`. Cache key: `listing:${params.listing_id}:${params.pms}`. Cache TTL: 5 minutes (300000ms). Extract base, min, max, currency from the listing response.
     b. If `start_date` AND `end_date` are provided, fetch existing overrides using `fetchWithFallback` with `/v1/listings/${params.listing_id}/overrides?pms=${encodeURIComponent(params.pms)}&start_date=${params.start_date}&end_date=${params.end_date}`. Cache key: `overrides:${params.listing_id}:${params.pms}:${params.start_date}:${params.end_date}`. Cache TTL: 5 minutes. Otherwise set existing_overrides to empty array.
     c. Build snapshot object: `{ snapshot_type: params.operation_type, listing_id: params.listing_id, pms: params.pms, captured_at: new Date().toISOString(), listing_state: { base_price: listing.base, min_price: listing.min, max_price: listing.max, currency: listing.currency }, existing_overrides: overrides }`.
     d. Store in audit log via `auditQueries.insertEntry.run({ action_type: 'snapshot', listing_id: params.listing_id, pms: params.pms, description: \`Pre-write snapshot before ${params.operation_type}\`, details_json: JSON.stringify(snapshot), channel: params.channel ?? null })`.
     e. Return the snapshot as JSON text content.
   - Wrap entire handler in try/catch. On error, return `{ error: message }` with `isError: true` (same pattern as audit.ts and analysis.ts).

2. Update `mcp-servers/pricelabs/src/index.ts`:
   - Add import: `import { registerOptimizationTools } from "./tools/optimization.js";`
   - Add registration call after the Phase 3 block, with comment `// --- Phase 4 tool registration (1 function, 1 tool) ---`:
     `registerOptimizationTools(server, db, apiClient, cache, rateLimiter);`
   - Update the module doc comment: change "23 tools (12 registration functions)" to "24 tools (13 registration functions)".
   - Update the inline comment on the tool registration block: change "12 functions, 23 tools total" to "12 functions, 23 tools" (Phase 1 count stays the same; Phase 4 adds its own section).
  </action>
  <verify>Run `npx tsc --noEmit` from `mcp-servers/pricelabs/` -- zero errors. Verify the index.ts imports and calls registerOptimizationTools. Count tool registration functions: should be 13 total (8 Phase 1 + 3 Phase 2 + 1 Phase 3 + 1 Phase 4).</verify>
  <done>pricelabs_snapshot_before_write tool registered and wired. TypeScript compiles. Server entry point loads the new tool. Total tool count: 24 tools across 13 registration functions.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors from `mcp-servers/pricelabs/`
2. `grep -c "registerTool\|server\.tool" mcp-servers/pricelabs/src/tools/*.ts` shows tool count increased by 1
3. `grep "snapshot" mcp-servers/pricelabs/src/schemas/monitoring.ts` confirms enum extension
4. `grep "registerOptimizationTools" mcp-servers/pricelabs/src/index.ts` confirms wiring
</verification>

<success_criteria>
- pricelabs_snapshot_before_write tool exists and compiles
- Audit log action_type enum includes 'snapshot' and 'rollback' in both input and query schemas
- Server entry point registers the new tool
- Total MCP tool count: 24
</success_criteria>

<output>
After completion, create `.planning/phases/04-write-operations-approval-workflow/04-01-SUMMARY.md`
</output>
