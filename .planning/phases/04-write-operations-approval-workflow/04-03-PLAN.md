---
phase: 04-write-operations-approval-workflow
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - openclaw/cron/jobs.json
autonomous: true

must_haves:
  truths:
    - "Weekly Monday cron job prompts include optimization scan instructions referencing the optimization skill"
    - "Cron prompts instruct the agent to detect orphan days, demand spikes, and check base price calibration"
    - "Both Slack and Telegram weekly jobs have identical optimization scan instructions"
    - "OPT-04 post-write verification confirmed existing in overrides.ts"
    - "OPT-08 min price floor validation confirmed existing in overrides.ts"
    - "OPT-09 currency matching confirmed existing in overrides.ts"
    - "TypeScript compiles with zero errors after all Phase 4 changes"
    - "Total tool count is 24 (23 existing + 1 new snapshot tool)"
  artifacts:
    - path: "openclaw/cron/jobs.json"
      provides: "Enhanced weekly cron jobs with optimization scan instructions"
      contains: "optimization skill"
  key_links:
    - from: "openclaw/cron/jobs.json"
      to: "skills/pricelabs-optimization/SKILL.md"
      via: "cron prompt references optimization skill protocols by name"
      pattern: "Orphan Day Detection Protocol|Demand Spike Detection Protocol|Base Price Calibration"
---

<objective>
Enhance the weekly cron job prompts to include optimization scanning and perform end-to-end verification of all Phase 4 deliverables.

Purpose: The weekly Monday report already runs at 10am and fetches the data needed for optimization detection. Enhancing the prompt to include optimization scanning avoids adding new cron jobs and additional rate limit consumption. E2E verification confirms all 10 OPT requirements are addressed (many by existing Phase 1 infrastructure).

Output: Updated cron jobs.json with optimization scan instructions. Verified Phase 4 completeness.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-write-operations-approval-workflow/04-RESEARCH.md
@.planning/phases/04-write-operations-approval-workflow/04-01-SUMMARY.md
@.planning/phases/04-write-operations-approval-workflow/04-02-SUMMARY.md

@openclaw/cron/jobs.json
@mcp-servers/pricelabs/src/tools/overrides.ts
@mcp-servers/pricelabs/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance weekly cron job prompts with optimization scanning</name>
  <files>openclaw/cron/jobs.json</files>
  <action>
Update the two weekly cron job entries in `openclaw/cron/jobs.json` (weekly-optimization-report-slack and weekly-optimization-report-telegram).

For BOTH jobs, replace the existing `message` field with an enhanced prompt that includes optimization scanning. The new message should be:

"Generate the weekly optimization report with pricing recommendations. Follow these protocols in order:\n\n1. Weekly Optimization Report Protocol (analysis skill): Compute portfolio KPIs with week-over-week and STLY comparisons. Detect underperforming listings and provide specific recommended actions.\n\n2. Orphan Day Detection Protocol (optimization skill): For each listing, scan the next 30 days for 1-3 night unbookable gaps between bookings. Exclude owner blocks (unbookable dates). Present specific fill strategies with recommended DSOs for any orphan gaps found.\n\n3. Demand Spike Detection Protocol (optimization skill): Scan the next 90 days for clusters of high-demand dates (red demand_color) without existing DSOs. Check if the algorithm has already elevated pricing before recommending. Recommend event-based percentage overrides where appropriate.\n\n4. Base Price Calibration Check (optimization skill): If it has been 30+ days since the last base price review for any listing, compare the listing's base price to neighborhood percentiles and recommended_base_price. Show full min/max price impact for any recommended changes.\n\nPresent the top 3-5 highest-priority recommendations that require approval. Follow the Recommendation Prioritization protocol from your optimization skill (orphan days first, then demand spikes, then base price). Log the report when complete."

Do NOT change:
- The `name` fields
- The `schedule` fields (keep existing cron expressions and stagger)
- The `sessionTarget` or `wakeMode` fields
- The `delivery` fields
- The `model` or `thinking` fields
- The daily health check jobs (leave them unchanged)
  </action>
  <verify>
1. `cat openclaw/cron/jobs.json | python3 -m json.tool` validates as proper JSON
2. Grep for "optimization skill" in jobs.json -- should appear in both weekly jobs
3. Grep for "Orphan Day Detection" in jobs.json -- confirms protocol references
4. Daily health check jobs are unchanged (grep for "daily-portfolio-health")
5. Both weekly jobs have identical message content
  </verify>
  <done>Weekly cron jobs updated with optimization scan instructions. Daily jobs unchanged. JSON valid. Both Slack and Telegram weekly jobs reference all three optimization protocols by name.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end verification of Phase 4 deliverables</name>
  <files></files>
  <action>
Run comprehensive verification checks across all Phase 4 deliverables. Do NOT create or modify any files -- only read and verify.

**Check 1: TypeScript compilation**
Run `npx tsc --noEmit` from `mcp-servers/pricelabs/`. Must pass with zero errors.

**Check 2: Tool count verification**
Count all tools registered across all tool files. Expected: 24 total tools across 13 registration functions.
- Phase 1: 8 registration functions (listings, prices, overrides, neighborhood, reservations, sync, rate-plans, status) -- count `server.tool(` and `server.registerTool(` calls in each
- Phase 2: 3 registration functions (snapshots, monitoring, audit)
- Phase 3: 1 registration function (analysis)
- Phase 4: 1 registration function (optimization)

**Check 3: OPT requirement coverage**
Verify each requirement is addressed:
- OPT-01 (recommend changes with rationale): Check skill Section 5 exists with recommendation format
- OPT-02 (require approval): Check skill Section 5 has approval flow with "approve"/"reject" language
- OPT-03 (snapshot before write): Check `pricelabs_snapshot_before_write` tool exists in tools/optimization.ts AND skill Section 4 mandates its use
- OPT-04 (post-write verification): Check overrides.ts has post-write GET verification (lines ~245-271)
- OPT-05 (orphan days): Check skill Section 1 has orphan day detection with 30-day window
- OPT-06 (demand spikes): Check skill Section 2 has demand_color-based detection
- OPT-07 (base price calibration): Check skill Section 3 has monthly neighborhood percentile analysis
- OPT-08 (min price floor): Check overrides.ts has price floor validation (lines ~205-221)
- OPT-09 (currency matching): Check overrides.ts has currency validation (lines ~143-203)
- OPT-10 (audit trail): Check skill Section 5 logs at each stage (recommendation, approval, execution) with before/after in details_json

**Check 4: Schema consistency**
Verify monitoring.ts has 'snapshot' and 'rollback' in both LogActionInputSchema and GetAuditLogInputSchema action_type enums.

**Check 5: Cron integration**
Verify jobs.json references optimization skill protocols and is valid JSON.

**Check 6: Skill quality**
Verify optimization skill has all 7 sections, references correct tool names, and includes pitfall mitigations:
- Unbookable date check (Pitfall 2)
- Algorithm pricing check before demand DSOs (Pitfall 3)
- 30-day base price interval (Pitfall 4)
- Min/max cascade display (Pitfall 5)
- Snapshot freshness re-check (Pitfall 6)
- No changes to booked dates (Pitfall 7)

Report the results as a verification matrix.
  </action>
  <verify>All 6 checks pass. All 10 OPT requirements have concrete coverage. TypeScript compiles with zero errors. Tool count = 24.</verify>
  <done>Phase 4 end-to-end verification complete. All 10 OPT requirements covered. TypeScript compiles. 24 MCP tools confirmed. Optimization skill has 7 sections with all pitfall mitigations. Cron jobs enhanced. Audit enum extended.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with zero errors
2. 24 MCP tools across 13 registration functions
3. All 10 OPT requirements have concrete coverage (3 new in Phase 4, 4 already existing from Phase 1, 3 addressed by skill protocols)
4. Cron jobs.json is valid JSON with optimization scan instructions
5. Optimization skill has 7 sections covering all protocols
6. All pitfall mitigations from research are reflected in the skill
</verification>

<success_criteria>
- Weekly cron jobs enhanced with optimization scanning (no new cron jobs added)
- All 10 OPT requirements verified as covered
- TypeScript compiles cleanly
- Tool count confirmed at 24
- Phase 4 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-write-operations-approval-workflow/04-03-SUMMARY.md`
</output>
