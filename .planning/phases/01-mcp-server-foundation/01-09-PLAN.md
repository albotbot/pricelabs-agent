---
phase: 01-mcp-server-foundation
plan: 09
type: execute
wave: 5
depends_on: ["01-03", "01-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "MCP server starts and responds to tool listing request"
    - "All 13 tools appear in tool discovery"
    - "API key is not exposed in any source file, config file, or log output"
    - "Domain knowledge skill file exists with correct metadata"
    - "OpenClaw config is valid and references correct MCP server path"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 deliverable: MCP server compiles and starts, all 13 tools are registered, security configuration is in place, and domain knowledge skill is complete.

Purpose: End-to-end validation that all Phase 1 requirements (INFRA-01 through INFRA-06) are met before marking the phase complete.
Output: Verification results confirming phase readiness.
</objective>

<execution_context>
@C:/Users/AIAutomations/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AIAutomations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-mcp-server-foundation/01-RESEARCH.md
@.planning/phases/01-mcp-server-foundation/01-08-SUMMARY.md
@.planning/phases/01-mcp-server-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 1 requirements</name>
  <files></files>
  <action>
Run a comprehensive verification suite. No files are created -- this is pure validation.

**INFRA-01: All 12 API endpoints wrapped as typed tools**
1. Build the project: `cd mcp-servers/pricelabs && npx tsc`
2. Verify compilation succeeds with zero errors
3. Grep all tool registration files for tool names. Expect exactly 13 tools:
   - pricelabs_get_listings
   - pricelabs_get_listing
   - pricelabs_update_listings
   - pricelabs_get_prices
   - pricelabs_get_overrides
   - pricelabs_set_overrides
   - pricelabs_delete_overrides
   - pricelabs_get_neighborhood
   - pricelabs_get_reservations
   - pricelabs_push_prices
   - pricelabs_get_rate_plans
   - pricelabs_add_listing
   - pricelabs_get_api_status
4. Verify each tool file imports from schemas/ (Zod validation)

**INFRA-02: Rate limiting**
5. Verify rate-limiter.ts exports TokenBucketRateLimiter
6. Verify constructor defaults are 1000 tokens, 3600000ms window
7. Verify api-client.ts calls rateLimiter.tryConsume before fetch

**INFRA-03: Caching**
8. Verify cache.ts exports TtlCache
9. Verify fetch-with-fallback.ts uses cache.get and cache.set
10. Verify different TTLs per tool type (grep tool files for ttlMs or TTL constants)

**INFRA-04: API key isolation**
11. Grep entire project for actual API key patterns (should find ZERO matches of real keys)
12. Verify index.ts reads from process.env.PRICELABS_API_KEY
13. Verify openclaw.json references ${PRICELABS_API_KEY} (env var syntax, not actual key)
14. Verify no skill file contains API key references
15. Verify openclaw.json has redactPatterns for API key

**INFRA-05: Gateway security**
16. Verify openclaw.json has gateway.bind: "loopback"
17. Verify openclaw.json has gateway.auth.mode: "token"
18. Verify channel configs have dmPolicy: "pairing"
19. Verify sandbox.mode: "all"
20. Verify tool deny list includes exec, write, edit, process, gateway

**INFRA-06: Domain knowledge skill**
21. Verify skills/pricelabs-domain/SKILL.md exists
22. Verify frontmatter has always: true
23. Verify all 5 sections present (optimization, algorithm, mistakes, API reference, portfolio context)
24. Verify file is 200+ lines

Report results as a checklist with pass/fail for each item.
  </action>
  <verify>
All 24 verification checks pass. If any fail, document the specific failure for remediation.
  </verify>
  <done>
All INFRA-01 through INFRA-06 requirements verified. 13 tools registered with Zod validation. Rate limiting at 1000/hr. Caching with per-endpoint TTLs. API key isolated to env vars only. Gateway secured with loopback + token auth + sandbox + pairing. Domain skill complete with all 4 knowledge domains.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies Phase 1 deliverable</name>
  <what-built>
Complete MCP server with 13 tools wrapping PriceLabs Customer API, token bucket rate limiter (1000/hr), TTL cache with per-endpoint configuration, OpenClaw Gateway security configuration, and domain knowledge skill.
  </what-built>
  <how-to-verify>
1. Check the MCP server compiles: `cd mcp-servers/pricelabs && npx tsc` -- should complete with no errors
2. Check the server starts (and exits cleanly without API key): `node dist/index.js` -- should print "FATAL: PRICELABS_API_KEY environment variable is required" and exit
3. Review `openclaw/openclaw.json` -- confirm gateway.bind is "loopback", auth.mode is "token", sandbox.mode is "all"
4. Review `openclaw/.env.example` -- confirm all required env vars are listed with descriptions
5. Review `skills/pricelabs-domain/SKILL.md` -- skim the content, confirm it covers optimization strategies, algorithm internals, common mistakes, and API field reference
6. Confirm no actual secrets in any committed file: `git grep -i "api.key\|secret\|token" -- ":(exclude)*.example" ":(exclude)*.md"`

If you have a PriceLabs API key ready, you can optionally test live by setting the env var and running the server -- but this is not required for Phase 1 sign-off. Live testing with real API calls is Phase 2.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md:
1. Agent can fetch listing data from PriceLabs API through MCP tools -- TOOLS EXIST (live API test deferred to integration)
2. Rapid successive API calls are throttled by the rate limiter -- RATE LIMITER IMPLEMENTED (1000/hr token bucket)
3. Repeated identical queries within cache TTL return cached responses -- CACHE IMPLEMENTED (per-endpoint TTLs)
4. API key does not appear in any skill file, agent log, or channel message -- ISOLATION VERIFIED (env vars only + redaction)
5. Unauthorized users cannot interact with the agent -- PAIRING MODE CONFIGURED (gateway + channel security)
</verification>

<success_criteria>
- All 24 automated checks pass
- User confirms MCP server compiles and starts
- User confirms security configuration is satisfactory
- User confirms domain knowledge skill content quality
- No secrets in committed files
</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-server-foundation/01-09-SUMMARY.md`
</output>
