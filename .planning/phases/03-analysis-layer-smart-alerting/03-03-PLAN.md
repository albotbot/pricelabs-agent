---
phase: 03-analysis-layer-smart-alerting
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "TypeScript compiles cleanly with all Phase 3 additions"
    - "Total MCP tool count is 23 (21 Phase 1-2 + 2 Phase 3)"
    - "All 4 cron jobs are valid and properly scheduled"
    - "Analysis skill is well-formed with all 6 required sections"
    - "New analysis tools reference existing database tables correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 3 end-to-end: TypeScript compilation, tool count, cron validity, skill completeness, and cross-plan integration. This is a non-destructive verification checkpoint that confirms all Phase 3 artifacts work together.

Purpose: Catches integration issues between Plan 01 (code) and Plan 02 (skill + cron) before marking Phase 3 complete. Prior phases used this same verification pattern (01-09, 02-06).

Output: Verification results confirming Phase 3 readiness, or specific issues to address.
</objective>

<execution_context>
@/home/NGA/.claude/get-shit-done/workflows/execute-plan.md
@/home/NGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analysis-layer-smart-alerting/03-01-SUMMARY.md
@.planning/phases/03-analysis-layer-smart-alerting/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end Phase 3 verification</name>
  <action>
    Run the following verification checks. Report results for each. If any check fails, diagnose and fix the issue inline (do not create a new plan).

    **Check 1 - TypeScript compilation:**
    Run `npx tsc --noEmit` from `mcp-servers/pricelabs/`. Must produce zero errors.

    **Check 2 - Total tool count:**
    Count all `registerTool` calls across all tool files: `grep -rn "registerTool" mcp-servers/pricelabs/src/tools/*.ts | wc -l`. Expected: 23.
    Also verify analysis tools specifically: `grep "registerTool" mcp-servers/pricelabs/src/tools/analysis.ts` should show `pricelabs_get_portfolio_kpis` and `pricelabs_detect_underperformers`.

    **Check 3 - Server wiring:**
    Verify `index.ts` imports and calls all registration functions including the new `registerAnalysisTools`. Count registration function calls: `grep -c "register.*Tools" mcp-servers/pricelabs/src/index.ts`. Expected: 12 (11 Phase 1-2 + 1 Phase 3).

    **Check 4 - Schema exports:**
    Verify `schemas/analysis.ts` exports both schemas: `grep "export const" mcp-servers/pricelabs/src/schemas/analysis.ts`. Expected: GetPortfolioKpisInputSchema, DetectUnderperformersInputSchema.

    **Check 5 - Query module exports:**
    Verify `db/queries/analysis.ts` exports createAnalysisQueries and row interfaces: `grep "export" mcp-servers/pricelabs/src/db/queries/analysis.ts`. Expected: PortfolioKpiRow, UnderperformerRow, MarketPositionRow, createAnalysisQueries.

    **Check 6 - Cron configuration:**
    Validate JSON: `node -e "const j=JSON.parse(require('fs').readFileSync('openclaw/cron/jobs.json','utf8')); console.log('Jobs:', j.length); j.forEach(j => console.log(' -', j.name, j.schedule.expr))"`.
    Expected: 4 jobs, daily at "0 8 * * *" (x2), weekly at "0 10 * * 1" (x2).

    **Check 7 - Skill file structure:**
    Verify analysis skill exists with correct frontmatter and section count: `head -10 skills/pricelabs-analysis/SKILL.md` and `grep -c "^## " skills/pricelabs-analysis/SKILL.md`. Expected: YAML frontmatter with `always: true`, 6+ section headers.

    **Check 8 - Tool name consistency:**
    Verify the tool names in the skill match the tool names registered in code:
    - `grep "pricelabs_get_portfolio_kpis" skills/pricelabs-analysis/SKILL.md` (must exist)
    - `grep "pricelabs_detect_underperformers" skills/pricelabs-analysis/SKILL.md` (must exist)
    - `grep "pricelabs_get_prices" skills/pricelabs-analysis/SKILL.md` (must exist, for demand calendar)
    - `grep "pricelabs_get_neighborhood" skills/pricelabs-analysis/SKILL.md` (must exist, for competitive position)
    - `grep "pricelabs_log_action" skills/pricelabs-analysis/SKILL.md` (must exist, for report logging)

    **Check 9 - ANLY-02 specificity audit:**
    Verify the skill contains specific action recommendations, not vague advice. Search for action patterns:
    - `grep -c "\\$\\[" skills/pricelabs-analysis/SKILL.md` -- number placeholder patterns should appear 10+ times
    - `grep -c "lower base price\|increase base\|expand.*discount\|listing audit" skills/pricelabs-analysis/SKILL.md` -- specific action verbs should appear 3+ times
    - Verify NO instances of vague standalone advice: check that "consider" and "may want" always appear with specific context, not as sole recommendation

    **Check 10 - No credential exposure:**
    `grep -rn "PRICELABS_API_KEY" mcp-servers/pricelabs/src/tools/analysis.ts mcp-servers/pricelabs/src/schemas/analysis.ts mcp-servers/pricelabs/src/db/queries/analysis.ts` should return zero results.

    Compile all results into a verification summary. If all 10 checks pass, Phase 3 is complete. If any fail, fix them inline and re-verify.
  </action>
  <verify>All 10 checks pass. Document results in the summary.</verify>
  <done>Phase 3 verification complete. All checks pass: TypeScript compiles, 23 tools registered, 4 cron jobs valid, skill has 6 sections with specific recommendations, tool names consistent between code and skill, no credential exposure.</done>
</task>

</tasks>

<verification>
All 10 verification checks from Task 1 pass without errors.
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- 23 total MCP tools registered
- 12 registration functions wired in index.ts
- 4 cron jobs (2 daily + 2 weekly) with valid schedules
- Analysis skill has 6 sections with specific actionable recommendations
- Tool names in skill match registered tool names in code
- No credential exposure in new files
</success_criteria>

<output>
After completion, create `.planning/phases/03-analysis-layer-smart-alerting/03-03-SUMMARY.md`
</output>
